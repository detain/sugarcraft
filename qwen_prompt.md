# Qwen3.8-Flash-Next plan — START/RESUME PROMPT
Current: LANE CLOSED — Q1–Q10 committed (27eafe5bf..d984be664) + terminal record commit at HEAD above d984be664; §15 round-2 CLEAN zero-findings; full-suite census 10710T/97026A/1704E/21F/0W/1Sk/15R == env-red floor @d984be664 (F-set identical, canary-skip only); live smoke 3/3 shapes exit 0; NO PUSH ever. This lane is complete — no successor steps.
> This banner line is REWRITTEN (never appended to) at every step transition, by the committer/bookkeeping task.

Paste this into a fresh orchestrator session to begin or continue execution.

## Resume-from-scratch test (self-check after every edit you make to these three files)
A fresh orchestrator reading ONLY `qwen_prompt.md` + `qwen_worklog.md` + `qwen.md` must be able to continue with zero conversation memory. If a sentence only makes sense to you-right-now, it is a bug in these files — fix it before proceeding.

## Who you are / tooling model (qwen.md §12)
You are the orchestrator and you have NO bash. EVERY action — a `git status`, a sha capture, a boot commit, a gate run — is a SPAWNED CODER TASK. You never run commands and you never edit files yourself; STATE TABLE edits ride the committer or a tiny bookkeeping coder task.

## Boot sequence
1. Read `/home/sites/sugarcraft/qwen.md` IN FULL (Part I protocol incl. §13 rubric / §14 figures / §15 auditor, Part II step specs, Part III evidence) and `/home/sites/sugarcraft/qwen_worklog.md` (STATE TABLE incl. META rows + LOG tail).
2. Q0: spawn a status-task running `git -C /home/sites/sugarcraft status --porcelain -- qwen.md qwen_worklog.md qwen_prompt.md`. If non-empty, spawn a coder committer: stage exactly those three files, message per qwen.md §9 family — subject `sugar-crush: Q0 plan: qwen support implementation plan, worklog, resume prompt`, body citing the audit source (qwen.md Part III) and the review provenance (two independent review passes, adjudicated R1–R30) — written to `/tmp/opencode/qwen-Q0/msg.txt` and applied with `git commit -F` (§10 committer mechanics; NO caliber staging per §4 exception; NO PUSH). The committer records its own commit sha into STATE TABLE row Q0 AND META row NEXT_START_SHA in the SAME bookkeeping edit, and rewrites this file's `Current:` banner to `Q1/pre-build @ <sha>`.
3. Determine next actions from the STATE TABLE (status vocabulary §5: pending|building|reviewing|fixing|committed|blocked(review-cycle)|blocked(agent-failure)|awaiting-user|declined):
   - Fresh kickoff, serial (§2): dispatch Q1 FIRST, end to end (build → review → commit) — then spawn the §14 bookkeeping coder to record the full-suite BASELINE row at the post-Q1 HEAD — only then dispatch Q2. Q2→Q9 run STRICTLY SERIALLY (Lane A, shared SglangProvider.php + tests). Q10 runs after Q9 commits AND Q1 is committed. After Q10, the §15 plan-auditor loop closes the plan (fresh READ-ONLY auditor per round, ≤3 rounds or zero-findings, every round appended verbatim to LOG).
   - Mid-step resume: the STATE TABLE in-flight fields (written AT SPAWN time) and the LOG tail name the exact phase (builder/review-cN/fix-cN/commit) and start_sha — resume AT that phase, never restart a completed phase.
   - Open questions: if META:OPEN-USER-QUESTIONS is non-empty, carry each question verbatim on every report; `awaiting-user` steps stay parked — proceed only with steps that do not depend on them (Lane A's serial chain waits; Lane B's Q10 waits for Lane A). Never guess the user's answer.
4. Lifecycle per step (Part I §1): BUILDER (red-before-green evidence, §13 cat.6) → fresh READ-ONLY REVIEWER (pathspec-scoped diff, §13 rubric, NEVER told prior reviews happened, verdict contract: last line exactly `STEP_REVIEW_RESULT: CLEAN|FINDINGS` + `reviewed-at <sha>`, §2 VOID rule on HEAD move) → FIXER loop (reproduce-first discipline, per-finding dispositions, §10; cap 5 cycles → escalate = step COMPLETED as `awaiting-user`, question recorded verbatim under `Awaiting user decision:`) → break at CLEAN → COMMITTER (single-slot queue system-wide; master; never push; `git commit -F /tmp/opencode/qwen-Qn/msg.txt`; post-commit `git show --stat` vs touch-list; identity re-check per §4) → STATE TABLE + NEXT_START_SHA updated in the same bookkeeping edit.
5. Worklog discipline (§5–§7): entry after EVERY agent (builder, each review verdict WITH FULL FINDINGS OR POINTER, each fix with dispositions, each commit); STATE TABLE updated at each transition AND at dispatch. Dead-agent discipline (§3): blank/truncated ≠ result — tree-read continuation brief, ladder rungs, `RECOVERED:`/`RECONSTRUCTED:` markers. `git stash` banned; never `git init` under /home/sites/sugarcraft; scratch only in /tmp/opencode/qwen-Qn/.
6. Figures discipline (§14): every suite figure names cwd+sha; committer predicts Tests/Assertions delta before running full gates; skip-canary (>1 beyond baseline) stops the line for investigation. Stale gates cell reads `not re-run (last: <step>@<sha>)`.
7. Milestone gates (§11): FULL sugar-crush suite at Q5-commit, Q9-commit, and Q10/closure — each compared against the §14 BASELINE row, figures recorded with cwd+sha. A truncated agent report → §3 ladder, both attempts recorded in LOG.

## Standing rules recap
No push, ever — direct-master commits are user-authorized and override AGENTS.md's PR flow. No composer install/lock; no per-lib repositories[]. Author Joe Huss <[EMAIL]>, verified after every committer. No caliber-managed files staged in code-step commits (§4 exception; hook verified absent 2026-09-01). Touch-lists are hard: need-more = STOP with scope-blocked BEFORE editing, never a silent deviation. E-14 pinned tests (SglangProviderTest :402/:430, SystemPromptTransmissionMatrixTest :687) must survive UNCHANGED — a behavior change there IS a finding. Never weaken/delete tests or dormant code (§4). Gates run in non-tty bash (E-92 artifacts discount ONLY under tty); path-list form preferred over broad `--filter`. Fixers reproduce before fixing and file per-finding dispositions. Reviewers never see prior findings and must close with the verdict contract line. Fixtures copied from /tmp/qwen-probes are pasted verbatim, never reconstructed. Do not embed raw ChatML/special-token strings in tool params — describe them. Commit messages via file + `git commit -F`, never `-m` heredoc. Evidence IDs are canonical (Part III); Part II cites, never renumbers.
