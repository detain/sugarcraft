# P3.audit-fix-3 — close the Phase 3 close-review cycle-2 findings — STEP BRIEF

## Role
You are the FIX agent for step P3.audit-fix-3. You implement, test, and commit to the branch.
You own this step's scratchpad root and spawn your own fresh READ-ONLY reviewers through your
own review cycles (cap 3 cycles; break early only when a cycle returns NO findings AND accounts
for every check). You NEVER merge to master — the orchestrator merges after your final report.

## Sandbox
Worktree `/home/sites/prompt-step-P3.audit-fix-3`, branch `prompt/P3.audit-fix-3`, branched at
master `3634aa1cb`. vendor/ is a hardlinked copy; PSR-4 verified printing the WORKTREE src/tests.
NEVER `composer install`/`update` here. CWD resets between your bash calls — absolute paths or
chained &&. For full-suite runs set your bash tool `timeout` >= 600000 ms.

## Scratchpad and identity
- Root `/home/sites/prompt-scratch/P3.audit-fix-3/` is YOURS; your working files live directly in
  it; every sub-agent you spawn gets its OWN subdirectory (`review-1/`, `review-2/`, ...) —
  ORCHESTRATION-RULE-3: no generic file names at shared roots, `rm -rf` only inside your tree.
- ORCHESTRATION-RULE-2: never `git init` or touch git config anywhere outside your scratchpad.
- F7 IS A LESSON ABOUT YOU: verify `git config user.email` prints `detain@interserver.net`
  (and user.name `Joe Huss`) BEFORE EVERY COMMIT. Your final report lists every commit sha you
  made with its author email. Do not repeat the gmail-address drift of the last audit lane.

## Required reading (before writing any code)
1. `/home/sites/sugarcraft/prompt_kit/findings/phase3-close-review-cycle-2-findings.md` — the
   seven findings with full six-field detail and measurements. Your work order is F1, F2, F4, F5,
   F6. F3 and F7 are bookkeeping/no-code (see dispositions).
2. `/home/sites/sugarcraft/prompt_kit/briefs/phase-review-brief-r2.md` — context (sandbox layout,
   suite rules, known-open list).
3. prompt_plan.md: §1.4 (366-501) esp. checks 14/16/19; §1.10 (827-873) removal is not an option;
   §1.11 (874-896) what counts as a test — deletion experiments MANDATORY for every guard you
   add or harden; §16.2 (2802-2877) golden-regeneration protocol; §16.8 rules 2, 13, 15, 16, 40,
   42 (3062-3256); §17 (3274+).

## Dispositions (orchestrator rulings — follow them; escalate a §1.10 conflict, never self-decide removal)
### F2 — HIGH — the thirteenth scanner defeat (src/Runtime.php `writePrimitivesCalledIn()` + tests/RuntimeTest.php)
Close the CONSTRUCTION channel: the scanner must see `new class(...) extends \SplFileObject {}`
(anon, where the extends clause names a write primitive directly — syntactically decidable), and
named subclasses DEFINED IN THE SAME FILE whose extends chain reaches a scanned primitive. The
miss path the reviewer measured: the extends-name token has `$next === '{'` (RuntimeTest.php:3723)
and no `T_NEW` precedes it (:3719). Every closure needs the M4-pattern deletion experiment:
break your own fix -> your new pin REDS -> restore. For what remains structurally unknowable
(a parent class imported from elsewhere or aliased), DO NOT fake detection — add an honest row
to the scanner's "WHAT THIS ALPHABET CANNOT EXPRESS" pinned list and say so in the report.
Rule from 5cabca4a8: add a spelling, never remove one. An unknown spelling must cost a false
POSITIVE, never a silent miss, wherever that is achievable by construction.
### F4 — MEDIUM — roster classifier alias escapes (tests/TreeWideGuardRosterTest.php)
The alias lesson this phase bled on never travelled to the roster classifier (rule 40). Make
`classifyWalkSites()` resolve `use function glob as g;` and `use RecursiveDirectoryIterator as
Walk;` (and the `new \SplFileInfo(...)->getChildren()` shape) so the three measured SILENT
escapes become detected or, where genuinely unresolvable, become PINNED declared-blind rows in
the blind-spot table (:1218-1284) — and correct that table's header claim so it no longer
asserts completeness it does not have. Include the addendum channel (class_alias of a walking
helper): detect or declare-with-pin. Derivation figures (roster/candidates/walkerFiles/
testFiles/unaccounted) will shift — state each new one and why.
### F6 — MODERATE — vacuous absence test (tests/Context/EnvironmentBlockTest.php:150-156)
`testNoAdditionalWorkingDirectoriesLineIsEmitted` passes on a renderer stubbed to `''` because it
has no known-positive control through the same scanner (rule 16). Restructure the test: the same
test (or an inseparable pair sharing one setup) must assert BOTH that the forbidden line is
absent from a REAL emitted block AND that the scanner detects a planted positive — so a dead
renderer reds it. Prove with the mutation yourself (stub render() -> the test REDS -> restore).
### F1 — MEDIUM — load flake in the shutdown test (tests/ClaudeCodeMcpClientShutdownTest.php:262)
The fixture writes its pid file (the only readiness handshake, :381) BEFORE `fwrite(STDERR, ...)`
(:382), and the test does one bounded non-blocking `drainStderr()` — a ~300 ms child stall makes
`stderrTail()===''` against correct library code. Fix the TEST, not the library (the 64 KiB cap
semantics stay byte-identical): replace the single bounded drain with a bounded POLL on stderr
DATA arrival, mirroring how `callTool()` polls `readMessages()` (src/ClaudeCodeMcpClient.php:728,
:885). Demonstrate determinism: run the focused test 10 consecutive times, paste every run.
### F5 — MEDIUM — the --root seam (src/Agents/Agent.php:916-920; tests/Agents/AgentTest.php:348)
The Agent assembler's `<env>` line reads `EnvironmentBlock::capture((string) getcwd(), ...)`,
while `Runtime::projectRoot()` uses `$app->root ?? getcwd()` — on a `--root <dir>` run whose
process cwd differs, per-stage sub-agent prompts name a directory the jailed tools do not share.
Wire the SAME root resolution into the Agent assembler (minimal consistent choice: pass the
resolved root at capture time — inspect how Agent reaches App/config and pick the cleanest
seam; do not duplicate the resolver logic a second time). Re-pin AgentTest:348 so root != cwd is
covered. GOLDEN CONSTRAINT: in both fixture contexts root == cwd, so BOTH goldens must stay
byte-identical (md5 32ea749d... / ef0326dd...). If any byte moves you MUST run §16.2 (old->new
md5, human-legible why-correct, leak scan) and flag it prominently — close-review cycle 3
re-checks this first.
### F3, F7 — no code
F3 was a defect in the orchestrator's brief text (claims: "all seven transmit" -> six + Echo
exempt by matrix assertion :348-353; "5,099 B" -> 5,176 B at HEAD); corrections travel with the
orchestrator's bookkeeping. F7 is un-rewritable history; your defence is the identity check
above. Do not touch either.

## Declared file list
`src/Runtime.php`, `src/Agents/Agent.php`, `tests/RuntimeTest.php`,
`tests/TreeWideGuardRosterTest.php`, `tests/Context/EnvironmentBlockTest.php`,
`tests/ClaudeCodeMcpClientShutdownTest.php`, `tests/Agents/AgentTest.php`, and — only if the F5
wiring genuinely needs it — `src/Workflows/WorkflowEngine.php`. Touching anything else: say so
under a 'scope widened' heading in the report with a one-line justification per file. If a fix
drifts toward a §1.10 conflict (dormant code in the blast radius), STOP that thread, record the
escalation verbatim in the report, and proceed with the rest — do not block, do not delete.

## Figures and suite rules
- Branch-point full-suite figure (inherited from master, measured): **10547 / 163710 / 1
  skipped**. Every test you add moves it — PREDICT your exact deltas in
  `<scratch>/prediction.md` BEFORE each full run, then reconcile: any unexplained movement is
  reconciled with the per-class JUnit diff (`/home/sites/sugarcraft/prompt_kit/tools/cmp.py`)
  BEFORE it is accepted. F1's test must not change counts, only reliability.
- Full suite: cwd = `/home/sites/prompt-step-P3.audit-fix-3` (checkout root), serial,
  `</dev/null`; state all three with every figure. Box-quiet probe first:
  `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` must print 0 (never the old bracket probe —
  it false-1s when run in the same command as a suite).
- Pre-checks (cheap, run them between edits): the nine-file census subset from the sandbox
  sugar-crush/ dir expect OK (176 tests, 31245 assertions + your declared deltas); the roster
  test file alone; the F2/F4/F6 target files alone.
- Two tests fail only under a live tty (CompactModelSummary, MouseModalGuard) — viewport
  artifacts; your non-tty runs must stay green; if they reproduce, stop and report, do not
  weaken anything.
- Commit to the branch EARLY and OFTEN (a commit is recoverable; a dirty worktree owned by a
  dead agent is not). Amend freely until a review cycle passes on it.

## Your review loop
After your own suite is green: spawn a FRESH read-only reviewer via your task tool
(subagent_type coder, READ-ONLY mandate: no edits/commits/config; experiments only with
restore) briefed with: this brief, the findings file, your diff range, the §1.4 19-check list
(re-derived at 366-501 — pass the CURRENT anchors), §1.11, and the demand for deletion
experiments on every guard this step touched. Reviewers never see your private reasoning, only
artifacts. Findings -> fix -> BRAND-NEW reviewer; cap 3 cycles; a blank/aborted reviewer
response means the reviewer DIED — resume it, never treat it as NO FINDINGS (§1.8 ladder,
5 attempts). Record cycle numbers and dispositions.

## Final report (your only deliverable; complete 7 sections)
1. Tree position + commit list (sha + author email each).
2. Per-finding disposition table (F1 F2 F4 F5 F6: what changed, which tests pin it, which
   deletion/mutation experiments proved the pins load-bearing — paste their RED output).
3. MEASURED figures with cwd/serial/</dev/null: full suite, per-file focused runs, the 10x F1
   loop, roster derivation new-vs-old, census subset, golden md5s (unchanged, or full §16.2
   package if moved).
4. Scanner: the new alphabet/coverage statement, the honest residual-unexpressible list you
   added, and one fresh defeat attempt you tried and either closed or documented.
5. Scope widened (if any) + §1.10 escalations (if any, verbatim).
6. What you did NOT do/check.
7. Review log: cycles, reviewer verdicts, findings disposition.

## Prohibitions (standing, unchanged)
Never push; never `caliber`; never `--no-verify`/hooks-path tricks; never `composer
install/update`; never modify git config; never weaken/skip/rename-out/delete an existing test;
never remove dormant code; `/usr/bin/grep` for tree scans; never touch `/home/sites/crush-lane-*`
or docs/plans/crush_code_*; strictly sequential tool use; intermediate messages 1-2 lines, no
narration; scratchpad files are your memory across interruptions. Begin by reading the findings
file and your step's diff base.

## SCOPE AMENDMENT (recorded post-merge by close-review cycle 3, 2026-09-02)

**WHAT THIS SAID:** nothing — the amendment was never made, which is the defect. The declared file
list above names EIGHT files (seven unconditional plus `src/Workflows/WorkflowEngine.php`, declared
conditionally for the F5 wiring). **WHAT IS TRUE NOW:** the merged window `3634aa1cb..60c037932`
touches NINE, re-derived at `cf41aacd6`:

```
$ git diff --name-only 3634aa1cb..60c037932 | wc -l
9
```

The eight this brief declares, plus `src/Cli/Bootstrap.php` — ONE line at :1240, `environmentRoot:
$root,` in the Agent construction call. So the widening is real and is exactly one file, one line;
any record that says the window touched "ten files" or that the declared list held nine names is
counting Bootstrap twice. **WHY IT STILL EARNS ITS PLACE:** the declared list is the instrument that
makes an out-of-scope edit visible, so it has to say what was actually in scope.

**Why the line was necessary:** F5's fix is ONE shared project-root resolution feeding BOTH prompt
assemblers. `Agents\Agent::systemPrompt()` cannot orient its `<env>` block without the resolved
root, and `src/Cli/Bootstrap.php` is the live call site that constructs the Agent — so the seam F5
closed was not closable without it. This is the same conditional this brief already grants
`WorkflowEngine.php` ("only if the F5 wiring genuinely needs it"), met: the line is work on F5, not
a stray edit. Nothing dormant was removed and no test was weakened (§1.10 untouched; the lane's
additive-only doctrine held), so rule 49 applies — a forced out-of-lane edit is reportable, not
prohibited.

**Where it was recorded:** close-review cycle 3 finding F3
(`/home/sites/prompt-scratch/P3.CLOSE-r3/review-1/findings-cycle-3.md`), MEASURED against the
sandbox diff, labelled documentation-class. The lane knew it was touching the file and mis-declared
it: the pickup fix-lane brief told its agent that "The step's declared-file list stands" and then
listed FOUR src files — `src/{Runtime,Agents/Agent,Workflows/WorkflowEngine,Cli/Bootstrap}.php` plus
the five test files — a list this brief never contained, asserted to stand against the very document
it cites (`/home/sites/prompt-scratch/P3.audit-fix-3/pickup/fix-agent-brief-template.md:12-13`). The
mis-declaration is why the widening never reached the authoritative plan-side record until now.
