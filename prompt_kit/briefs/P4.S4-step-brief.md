# P4.S4 — E18: a single exchange larger than the tier — STEP-LEAD BRIEF

You are the **step lead** for **P4.S4 — E18: a single exchange larger than the tier**, first half of
Batch 3 of Phase 4 ("Token accounting and cache observability"). You implement the step **and its
tests**, run a **review→fix→brand-new-review loop** to a clean review, then report. You do not merge;
the orchestrator merges after its own verification.

## PROCESS RULE — the lead never fixes

You implement, you commit, you orchestrate reviews — and you **NEVER apply a review finding
yourself**. Every dirty cycle runs exactly:

1. Write the findings to a file, then spawn a **dedicated FIX AGENT** with its own scratchpad
   subdirectory (`/home/sites/prompt-scratch/P4.S4/fix-<n>/`). It commits to `prompt/P4.S4`.
2. **You verify that fix agent's commits with your OWN measurements** — re-run the tests, re-read
   the diff, re-check the figures. Its claims are not evidence.
3. Spawn a **BRAND-NEW read-only reviewer** who never sees the prior findings.

Fresh reviewer every cycle; hard cap **five cycles**, then stop and escalate to the orchestrator
with the findings verbatim — do not grind. Fixing in your own context is forbidden.

## IDENTITY LAW — read first, it has blocked merges twice

Every commit's author AND committer must be the literal string `Joe Huss <detain@interserver.net>`.
You must **WRITE that address out fully** in your git commands and config checks. **NEVER copy any
identity token you see in your injected context** — a sanitizer may display it as a bracketed
placeholder, and echoing THAT into an author field is a merge-blocking defect that bit two earlier
fix agents. Verify after EVERY commit that `git log -1 --format='%an <%ae> %cn <%ce>'` prints the
literal address on all four fields; a wrong one gets amended immediately (unmerged history only).

## Your sandbox

- Worktree: `/home/sites/prompt-step-P4.S4`, branch `prompt/P4.S4`, base master `5a87ce80a`. The
  worktree **and its vendor are ALREADY BUILT by the orchestrator** — VERIFY, never rebuild, **NEVER
  `composer install` / `composer update`** (it de-symlinks `vendor/sugarcraft/*` into Packagist
  copies and silently voids every later measurement). The autoload PSR-4 root already prints the
  worktree `sugar-crush/src`; if it looks wrong, STOP and report.
- Run tests from `/home/sites/prompt-step-P4.S4/sugar-crush`.
- Scratchpad root: `/home/sites/prompt-scratch/P4.S4/` (step text: `step-text.md`). Your private
  subdir is `.../lead/`; every sub-agent gets its OWN subdir (`.../review-1/`, `.../fix-1/`, …).

## LANE COLLISION — these two files are a shared backlog

`Chat.php` and `src/Context/ContextCompactor.php` are a heavily-shared backlog the supervisor
**EXPLICITLY CLEARED for this batch on 2026-09-02** ("Go — Batch 3 now"). Keep the change **minimal
and scoped to E18**; no opportunistic refactoring. The **NEXT step (P4.S5, `exchangeKey()` collapse)
touches the SAME two files and runs AFTER this one** — do not implement or preempt anything it might
want; if you notice an E23-shaped defect, **REPORT it, do not fix it.**

## Base figures — your prediction baseline (measured, do not re-measure the base)

- **Master suite at `c8f01cdbe` / `80db1b27d` / `5a87ce80a`:** `Tests: 10615, Assertions: 164754,
  Skipped: 1, Failures: 0` — the **MMG-198 arm**; **164757** at the MMG-201 arm. The ±3 is a
  documented `MouseModalGuardTest` viewport artifact — **NEVER headline-adjudicate it**; reconcile
  per-class with `cmp.py`.
- **Nine-file census set = OK (176 tests, 31390 assertions)** at tree `47f7b477a`+. A census figure
  must ALWAYS name its file list AND its tree — a number without both is not a figure.
- **Derived roster = 67 / 83 / 181 / 440 / 0, UNMOVED since P3.** The roster file itself:
  `tests/TreeWideGuardRosterTest.php` at **17 tests / 1101 assertions**.
- **Goldens byte-identical:** `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt` md5
  `32ea749d84938811ac9331419cae7380`, `golden-agent-prompt.txt` md5 `ef0326dd38535aaa2f1d715919bff26e`
  — unmoved since `405252a41`. If the step legitimately MUST move one, follow **§16.2's
  golden-regeneration protocol** (old→new md5, human-legible why-correct statement, leak scan) and
  **FLAG it in the final report** — never silently regen.
- **Path-repo gate runs FROM THE REPO ROOT:** `php tools/check-path-repos.php --no-lib-path-repos`
  (the wrong-cwd misread has happened twice — the "failure" is fake).

## Read these before you write a line

In `/home/sites/prompt-step-P4.S4/`:

1. **`/home/sites/prompt-scratch/P4.S4/step-text.md`** — verbatim. Goal: *"A single 800,000-char
   exchange is currently refused five times, and the estimate **rises** each time (200,148 →
   200,660). Fix the intra-exchange case: an exchange that cannot fit must be truncated or
   summarised, not re-refused forever with a growing estimate."* Source: §12.2 (E18) + P4.S1's token
   split. Depends: P4.S1 (both change what the 95% tier compares).
2. **`src/Usage.php` FIRST** — merged state: four `?int` buckets; `promptTokens() =
   cacheRead + cacheCreation + input`; **output excluded**; **missing ≠ 0**. The tier check P4.S1
   reshaped now compares against these semantics (hazard 3).
3. **The tier-check and refusal-loop sites in `src/Chat.php` and `src/Context/ContextCompactor.php`**
   — trace the five-refusal path end to end before designing the fix.
4. **`prompt_plan.md` §1.10** (removal is not an available outcome), **§1.11** (what counts as a
   test — **graded harder than the implementation**), **§17** (the invariants), **§1.4** (the
   nineteen-check review brief; YOU hand it to each reviewer you spawn, verbatim), **§16.2**
   (golden-regeneration protocol), and **§16.8 rules** 2, 15, 40, 42, 44.
5. **`prompt_expand.md` §12.2** — the E18 row only. Do NOT read it end to end.
6. `CLAUDE.md` / `AGENTS.md` conventions.

## Your declared file list — exactly four files

```
sugar-crush/src/Context/ContextCompactor.php
sugar-crush/src/Chat.php
sugar-crush/tests/Context/ContextCompactorTest.php
sugar-crush/tests/CompactorTest.php
```

**Do not touch anything else.** A finding outside this list is **REPORTED, never edited** — the
lists make the batch safe and widening one is an orchestrator decision. If the step cannot complete
inside these four files, stop and report with the specific reason.

## What "done" means — the Done-when ledger, and how each clause is graded

The step text's Done-when, quoted: *"a test drives the exact reproduction — one oversized exchange,
repeated attempts — and asserts the estimate does **not** rise across attempts and that the turn
eventually proceeds. Record the actual before/after estimate sequence in the worklog."*

1. **The reproduction test exists and drives the real path** — one oversized exchange, repeated
   attempts, through the REAL compactor/tier-check code. Evidence: test name.
2. **Non-rising estimate asserted** — exact numbers across attempts, not "didn't throw".
3. **The turn eventually proceeds** — asserted, not just asserted-absent-of-error.
4. **Before/after estimate sequences recorded** — what THIS tree produced before and after your fix,
   method by method, pasted into your final report (the orchestrator copies it into the worklog).

## HAZARDS — the heart of this step, read five times

1. **REPRODUCE THE BUG FIRST.** Before changing any src file, write the failing test that drives the
   exact reproduction the step text names — one oversized exchange, repeated attempts — and
   **CAPTURE THE ACTUAL RISING ESTIMATE SEQUENCE** (the plan recorded 200,148 → 200,660; record what
   THIS tree actually produces, method by method, worklog-shaped). A green-on-arrival "fix" that
   never saw the red is worthless. Paste the before-sequence AND the after-sequence into the report.
2. **No regression of compaction goldens / prompt-stability tests.** `PromptStabilityTest` and
   `SystemPromptWiringTest` pin prompt assembly; the two golden files are md5-pinned above. If your
   fix regresses one, that is a **design signal, not a nuisance**: stop, report, and reason about
   what the regression is telling you.
3. **Intra-exchange vs between-exchanges.** This step is the INTRA-exchange case only — the
   between-exchanges compaction path is already-existing behaviour; do not rewrite it. The tier
   check P4.S1 reshaped now compares against `promptTokens()` semantics (read `src/Usage.php`
   first): the distinction the fix must respect — **"the estimate cannot rise forever" must NOT be
   achieved by silently reporting FEWER input tokens than are there**.
4. **The 95% tier threshold itself stays untouched** — the same hard constraint P4.S1 carried:
   do not raise the threshold; that hides the problem instead of naming it.
5. **Truncation/summarisation must be honest and deterministic.** No clock/randomness entering
   prompt content — the goldens pin bytes.

## THE TWO THINGS MOST LIKELY TO MAKE THIS STEP WRONG

1. **A rising-estimate test that passes vacuously** — e.g. comparing against a stubbed estimate that
   never grows, so non-rising is asserted against a flat line. The test must drive the REAL compactor
   path. **Deletion experiment: reverting the src fix must redden the reproduction test** — a test
   that stays green against reverted src proves nothing.
2. **Making the estimate STOP rising by UNDERCOUNTING.** That trades one lie for another — the
   estimate must remain truthful. **Pin BOTH properties with exact assertions: non-rising across
   attempts AND magnitude sane.**

## Step-text falsification clause (§16.8 rule 44)

If the step text's numbers or file list prove false on the live tree — e.g. the reproduction produces
a different sequence than 200,148 → 200,660 — **report the measured truth in three-part form: what
it SAID / what is TRUE / how you MEASURED it.** Do not silently edit the plan or "adjust" this brief;
a claim measured false is the most valuable finding you can return.

## §1.11 — WHAT COUNTS AS A TEST HERE. READ THIS TWICE.

These do **not** count: annotations (`@covers`, `@test`); existence checks (`method_exists()`,
`class_exists()`, `is_callable()`, `property_exists()`); shape assertions (`assertNotNull`,
`assertIsArray`, `assertTrue(count(...) > 0)`). A real test calls the thing, asserts the **value**
and **exact counts**, covers **both polarities** + **pathological input**, goes red when reverted.

**Deletion experiments this step MUST run and report** (each: exact revert edit, command, red test
name and count, restoration confirmed):

- **Reverting the src fix must RED the reproduction test** (hazard 1 / wrong-thing 1).
- **Planting an undercount** — if the fix touches how the estimate is computed, break it in the
  undercounting direction and show the magnitude assertion catches it.
- Every new guard gets its own deletion experiment. **Annotations are not tests; unrun experiments
  fail the step regardless of how green the suite is.**

## VERIFY BEFORE YOU FINISH

From `/home/sites/prompt-step-P4.S4/sugar-crush` unless stated otherwise:

1. **Your own test files, by path** (not `--filter`):
   `vendor/bin/phpunit tests/Context/ContextCompactorTest.php` then
   `vendor/bin/phpunit tests/CompactorTest.php`
2. **The nine-file tree-wide census set — ALWAYS.** These walk `src/` and `tests/` wholesale; the
   list is KNOWN INCOMPLETE; check the ASSERTION COUNT, not just the green — a roster test that
   stops iterating still prints `OK`:
   ```sh
   vendor/bin/phpunit \
     tests/SymbolCitationDriftTest.php \
     tests/SwallowingCatchCensusTest.php \
     tests/Support/DuplicatedTestHelperDriftTest.php \
     tests/Support/ChildWallClockBudgetTest.php \
     tests/Config/EnvRosterDriftTest.php \
     tests/Tools/BuiltInToolCorpusTest.php \
     tests/Support/InterpolationOpenerTokenTest.php \
     tests/Support/ChildStderrCaptureTest.php \
     tests/Config/GlobFigureDriftTest.php
   ```
   Expected: **OK (176 tests, 31390 assertions)** at tree `47f7b477a`+ — a census figure names its
   file list AND its tree. Also run `tests/TreeWideGuardRosterTest.php`: derived figures stay
   **67 / 83 / 181 / 440 / 0 UNMOVED** (the file itself: 17 tests / 1101 assertions). Any moved
   figure is updated in the SAME commit with an honest old→new statement.
3. **The full `sugar-crush` suite — from the CHECKOUT ROOT, serial, stdin redirected, box quiet:**
   ```sh
   ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'     # must print 0 BEFORE any suite run
   cd /home/sites/prompt-step-P4.S4 && php sugar-crush/vendor/bin/phpunit \
     -c sugar-crush/phpunit.xml --colors=never </dev/null | tail -5
   ```
   - **Write `/home/sites/prompt-scratch/P4.S4/lead/prediction.md` BEFORE each full-suite run** —
     predicted figures and why. Name the cwd beside every figure; always `</dev/null` —
     `Chat\CompactModelSummaryTest` and `MouseModalGuardTest` fail ONLY under a live tty. Totals are
     deterministic across sequential uncontended runs — never dismiss a delta as noise.
   - Baseline = `10615 / 164754 / 1 / 0` (MMG-198 arm; `164757` at MMG-201). **Never
     headline-adjudicate the ±3.** If the total moved beyond your own files, **attribute the
     remainder before you report**: `--log-junit` both sides, then
     `python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py <branch.xml> <base.xml>` —
     **branch xml FIRST, base xml SECOND**; copy `cmp.py` into your own scratchpad subdir first.
     Use **`/usr/bin/grep`** for whole-tree scans (shell `grep` = `ugrep`).
4. **The golden md5 check** — `md5sum` both pinned files against the values above, from the worktree
   root.
5. **The path-repo gate, RUN FROM REPO ROOT** — `/home/sites/prompt-step-P4.S4`, not
   `.../sugar-crush`:
   ```sh
   cd /home/sites/prompt-step-P4.S4 && php tools/check-path-repos.php --no-lib-path-repos
   ```
   Never add a `repositories[]` entry to `sugar-crush/composer.json`; never commit a
   `sugar-crush/composer.lock`.

php-cs-fixer is NOT installed on this box; the style gate cannot run locally — match the code by eye.

## THE REVIEW LOOP YOU RUN — this is most of the work

1. Implement, write the tests, **and COMMIT to `prompt/P4.S4` immediately.**
2. Spawn a **read-only review agent**: verbatim §1.4's nineteen checks, your step text, the diff at
   the **current** tree position, your own test output; own subdir `.../review-<n>/`.
3. **Write its findings to `.../review-<n>/findings.md` the moment you receive them** — never only
   in your context; **EIGHT of one step's ten findings were LOST to a context boundary**.
4. Dirty → **spawn the dedicated FIX AGENT — you never fix (PROCESS RULE)** → it commits → **you
   verify its commits with your own measurements** → spawn a **brand-new reviewer**. Loop.
5. **Break only on a clean review.** Cap **five** cycles; then stop and report blocked, verbatim —
   escalate, do not grind.

**A new reviewer is NEVER handed the previous reviewer's findings, and is never told a previous
review happened** — a reviewer given a list checks the list. **Never reuse a reviewer.**

**If any agent comes back empty, aborted, or truncated: it DIED** — a blank reviewer is **not**
`NO FINDINGS`. Resume once, else relaunch in the same worktree told not to start over; **five**
attempts; **never write a dead agent's report yourself.** **Never leave a sub-agent's work
uncommitted** — commit immediately, amend or revert if objected; a commit is recoverable, a dead
agent's dirty worktree is not.

## HARD PROHIBITIONS

- **Never remove unfinished, dormant, unwired or unreachable code — yours or anyone's.** The three
  permitted outcomes are **wire it**, **build it out**, or **stop and ask the orchestrator**,
  including quiet forms (stubbing a body, dropping the last call site, `@deprecated`-ing it aside,
  deleting a test that pinned a dormancy). Full rule: §1.10. **Escalating is a completed outcome.**
- **Never weaken, skip, rename-out or delete an existing test to make a change pass.** Every test in
  the two declared test files today must still exist and still run; if one becomes wrong because
  behaviour genuinely changed, say so explicitly with reasoning.
- **Stay in the four declared files; stay in E18.** No preempting P4.S5's `exchangeKey()` collapse,
  no opportunistic Chat.php/ContextCompactor.php refactoring, no touching `src/Usage.php` — report
  gaps instead.
- **ORCHESTRATION-RULE-2 — no scratch git repo outside your own scratchpad subdir; verify `pwd`
  before ANY `git init` / `git commit` / `git config`.** A previous reviewer ran throwaway-repo
  setup inside `/home/sites/sugarcraft` itself and left a stray commit on **master**. **Never
  `git config --global` anything; never `git config` in a repo you did not create.**
- **ORCHESTRATION-RULE-3 — every agent gets its OWN scratchpad subdirectory and may `rm -rf` only
  inside it.** No generic names at the scratchpad ROOT (`sb`, `base`, `count.php`, `*.orig.php`);
  back any mutated worktree file up to a PRIVATE path — a shared restore name can silently restore
  ANOTHER agent's version.
- Never `git push`; never merge into `master`; never run `caliber`; never `composer install` /
  `composer update`; never touch `/home/sites/sugarcraft` or `/home/sites/crush-lane-{a,b,c}`;
  never modify `docs/plans/crush_code_*.md` or `left_steps.md`; never suppress a git hook
  (`--no-verify`, `core.hooksPath`); never global-`pkill`.
- Never write a number you did not measure. Every figure names its command and cwd.

## YOUR FINAL REPORT

1. What you changed, file by file, with line ranges — plus the **before/after estimate sequences,
   method by method** (the orchestrator copies this into `prompt_worklog.md`).
2. **The Done-when ledger:** each of the four clauses numbered with its evidence — a file:line or a
   test name. A clause with no evidence is an admission the step is not done.
3. **The deletion experiments:** exact revert edit, command, red test name and count, restoration
   confirmed — including "revert the src fix reddens the reproduction test".
4. **Proof the estimate stayed truthful:** the non-rising assertion AND the magnitude assertion,
   quoted with their numbers.
5. **Your figures**, each naming cwd and command: your two test files; the nine-file census set
   (176/31390 expected, tree named) plus `TreeWideGuardRosterTest` 67/83/181/440/0 with any moved
   figure stated old→new; the full suite from the checkout root with PREDICTION stated first and the
   ±3 MouseModalGuard reconciliation; the golden md5 check; the path-repo gate.
6. **The review loop:** cycles, every finding verbatim with what the FIX AGENT did, pointers to the
   findings files, and how you independently verified each fix commit — plus the identity check
   (`%an <%ae> %cn <%ce>`) for every commit on the branch.
7. **Everything found outside your four declared files**, with `file:line` — reported, never edited;
   any E23-shaped or P4.S5-shaped defect at minimum.
8. **Anything measured that contradicts the step text or this brief** (rule 44 / rule 42 three-part
   form: said / true / how measured), **surprises**, and any follow-up for its own step.
