# P6.S3 — Rulebooks: named, toggleable rule packs (step brief)

**Plan reference** `prompt_plan.md:2208-2280` (section as amended 2026-09-05; the Phase-6 concurrency correction sits at `:2324-2336`).
**Sources** §9.13, premise check `armed-harlequin-wolverine` (2026-09-05). **Depends on** P6.S2 (merged). **Ruling date** 2026-09-05.
**Sequence ruling: S3 then S4, serially — never in parallel.** The plan's old "run P6.S4 concurrently with P6.S3 (disjoint)" was false on measured file overlap and is corrected in place.

Read before coding: `sugar-crush/src/Context/RuleLoader.php` (the class doc-block IS the spec — containment, the two dedup passes, the three caps), `sugar-crush/src/Context/Rule.php`, `sugar-crush/src/Commands/AgentsCommand.php` (the helper-class idiom you must copy), `sugar-crush/tests/Context/RuleLoaderTest.php` (the temp-HOME render pattern), and `prompt_plan.md` §1.10 / §1.11 (plan lines 827 and 874).

## Step text (verbatim from the plan)

> **Goal** `~/.sugar-crush/rulebooks/*.md` — named packs with a `/rules <name>` command to toggle.
> **Done when** toggling a rulebook changes the assembled prompt, asserted by a golden diff, and the
> toggle persists across a session restart (or explicitly does not, and a test pins which).

The RULING in the plan beneath this text is **newer than the step text and is the spec.** Where the two collide — "asserted by a golden diff" — the ruling governs: the expected golden move here is **zero**, and the two-render assertion replaces the diff. Read the whole RULING block (`prompt_plan.md:2226-2280`) before you write a line.

## Declared file list

- `src/Context/RuleLoader.php` — rulebooks directory, per-directory read counter (UNCHANGED semantics — see requirement 8), NIT-5 refusal, doc-block prose.
- `src/Commands/CommandRegistry.php` — the `/rules` row.
- `src/Commands/RulesCommand.php` (new) — helper class.
- `src/Chat.php` — dispatch arm + handler. Unavoidable: `SlashDispatchTest` reds on a registry row with no live branch.
- `src/App/App.php` — the disabled-pack set rides here, mirroring `enabledSkills`.
- `src/Runtime.php` — the splice reads it.
- `src/Backend/EngineBackend.php` — per-turn carry from Chat to App.
- `tests/Commands/RulesCommandTest.php` (new) and `tests/Context/RuleLoaderTest.php`.
- Pre-authorized census pair: `src/Cli/Bootstrap.php`, `tests/Cli/ProjectTierRefusalInventoryTest.php`.

## NOT in this step (hard scope boundary)

`src/Context/PromptFence.php` · `src/Skills/Skill.php` (P7.S4 owns `matchesPrompt`) · `tests/Providers/PromptStabilityTest.php` ·
`tests/fixtures/prompt/**` and both golden files ·
`src/Context/Sections/MaximsSection.php` + `tests/Context/Sections/MaximsSectionTest.php` (their ordinal prose is a deferred carry-forward for whoever next owns those files — expressly not this step) ·
`tests/MouseModalGuardTest.php` (**path corrected** — it is not under `tests/Tools/`; its 198/201 viewport alternation is legitimate, never "fix" it) ·
`/home/sites/crush-lane-*`, `docs/plans/crush_code_*.md`, `left_steps.md` — read-only to this plan. Note as measured: no `left_steps.md` exists at this tip; create nothing in its name.
Persistence keys, `src/Config/LayeredSettings.php`, and any `rules:` config surface belong to **P6.S4**, which runs after this merges.

## Rulings — already decided; do not re-litigate

- **A1 as written:** rulebooks live at `~/.sugar-crush/rulebooks/*.md`, one file = one pack, identity = basename minus `.md` — which is exactly what `ruleKeyFor()` (`RuleLoader.php:552-558`) already produces for a flat directory. No new identity function.
- **Tier:** reuse `user`; no fourth `TIERS` value.
- **Interaction:** frontmatter `enabled:` AND the runtime disabled-set, with frontmatter winning.
- **Persistence:** out of scope; session-scoped only, pinned by a byte-identical `config.json` test.
- **Goldens:** zero movement expected.

## Precedent reality

`RuleLoader` mirrors `CommandLoader`'s refusal-recording and realpath-dedup discipline, **not** its tiers, and the user anchor is deliberately tighter (`$HOME/.sugar-crush`, not `$HOME`) for a reason stated in the class doc-block. The command idiom to copy is `AgentsCommand` + its `Chat` handler, not `Help`-style inline branches. The state-propagation precedent to copy is `enabledSkills` end to end, including the `EngineBackend` carry.

## Mandatory API choice

Reuse, never re-spell: `loadFromDirectory()` (`RuleLoader.php:406`) for the walk · the existing `anchoredIn` gate at `:413`/`:450` for containment · `refusedPaths()` (`:370`) for the NIT-5 record · `report()` (`:579-584`) for any message · `Rule::withEnabled()` (`Rule.php:208`) for the toggle value.

## GUARD TRIPWIRES — all DERIVED ledgers; do NOT read any of them whole

`ContainedPathInventoryTest` (37/15 — see requirement 1) · `ReadPathCensusTest` (64 rows / 90 verdicts) · `ProjectTierRefusalInventoryTest` (requirement 10) · `StderrEmitterCensusTest` (`:295` already allows RuleLoader exactly 1) · `EnvRosterDriftTest` · `DuplicatedTestHelperDriftTest` · `AssertionSwallowingCatchTest` (3 assertions per `try`) · `SlashDispatchTest` (must pass untouched) · `BaseSystemPromptTest` (the 7,829-byte pin at `:835`, `$expected` map at `:997-1005`, tripwire at `:1006-1010`).
Every figure above was measured at `1695a6afe`; the last two steps each moved one of them on unrelated grounds, so re-measure before you predict.

## §1.10 — removal is not an available outcome

`Rule::withEnabled()` (`src/Context/Rule.php:208`) is the dormant primitive this step EXISTS to adopt — its only caller today is `tests/Context/RuleLoaderTest.php:458`. Calling it is the plan working. Deleting, stubbing, deprecating or "simplifying" it, or anything else dormant you trip over, is a rejection of the plan: escalate instead.

## §1.11 — what counts as a test

A real test goes red when the change is reverted. Run and paste the deletion experiments for all six: the rulebooks directory load, the toggle filter, the `/rules` dispatch arm, the AND-semantics, the dangling-symlink refusal, and the per-directory cap. Each needs the red output and the green-after-restore output.

## Hard requirements

1. `~/.sugar-crush/rulebooks/*.md` loads through the EXISTING `loadFromDirectory()` and the EXISTING user anchor. Measured consequence: **+0** `ContainedPath` call sites (ledger text says THIRTY-SEVEN in FIFTEEN at `src/Support/ContainedPath.php:97`; `tests/Support/ContainedPathInventoryTest.php:326-332` spells the map only up to THIRTY-EIGHT — so 38 moves the spelled doc-block and **39 breaks the test's own `$words` map**), and **+0** read sinks (`tests/Support/ReadPathCensusTest.php:122`: 64 rows / 90 verdicts, exact-equality derivation). The anchor is free because `loadUserRules()` (`:279`) already passes the PARENT `$HOME/.sugar-crush` as `anchoredIn` (`:294-298`). WARNING: add your own containment gate instead of reusing the loader's and you walk straight into the 39 overflow.
2. **No new tier.** `Rule::TIERS` (`src/Context/Rule.php:77`) stays `['user','project','root']`; rulebook-sourced rules carry tier `user`. Say the provenance reason in the doc-block — `~/.sugar-crush/rulebooks/` is operator-controlled by definition — so no reviewer reads it as a shortcut. `<user-rules>` plus `USER_RULES_AUTHORITY_PREAMBLE` (`src/Runtime.php:131`, spliced at `:2579`) is the honest frame.
3. A `/rules <name>` command: registry row in the existing `CommandSpec::new(...)` shape (`CommandRegistry.php:84+`), a `Chat::dispatchCommand` match arm (`src/Chat.php:6758`; the `agent` arm at `:6821` is the model) and a handler following the `AgentsCommand` idiom — `final class`, `execute(Chat $chat, array $args = []): int` (`src/Commands/AgentsCommand.php:83`), output captured by `ob_start()`/`ob_get_clean()` in the Chat handler (`:8290`), non-zero exit routed through `commandFailureResponse()` (`:8259`). **`tests/Commands/SlashDispatchTest.php` must pass without editing it** — that is the machine's proof the command is genuinely wired rather than registered-and-dead. `rules` is NOT among the seven reserved `CONTROL_PLANE` names (`CommandRegistry.php:68`; the enforcement loop is `CommandLoader.php:504-528`), so add nothing there.
4. **Sub-commands and no-args behaviour defined AND tested:** bare `/rules` lists packs with their on/off state; `/rules <name>` toggles; an unknown name returns an error response, never a silent no-op. Pin both polarities (on→off and off→on).
5. **Session-scoped, pinned.** State crosses Chat→App→Runtime the way `enabledSkills` does: `App.php:83`, the `withEnabledSkills()` mutate at `:238`, consumed at `Runtime.php:2646`, carried per turn at `EngineBackend.php:556`. The done-when's second branch is satisfied by a test asserting `config.json` is **byte-identical** after a toggle — the persistence refusal is a pinned fact, not an omission. `onConfigChange` must NOT be called.
6. **AND-semantics test:** a pack whose frontmatter says `enabled: false` stays off under an on-toggle.
7. **NIT-5:** a dangling `*.md` symlink inside `rules/` or `rulebooks/` currently vanishes at `RuleLoader.php:435` via `!$file->isFile()` with **zero** ledger entries. Make it a recorded refusal with a reason, and use the accessor the code supports for it: `refusedPaths()` (`:370`), because the doc-block reserves `skippedFiles()` (`:381`) for cap trips and parse errors — truncation of content that did resolve — while a path whose `realpath()` fails cannot be shown to stay inside the anchor. This is the plan's standing rule that the scanner fails CLOSED and an unknown spelling costs a false positive, never a silent miss. Plus a test.
8. **`MAX_FILES` is PER-DIRECTORY — semantics UNCHANGED, deliberately** (`:129`, 64; the counter bounds reads in EACH directory walked, not one build-wide total). SUPERSEDED 2026-09-05: the first ruling said the cap "stays GLOBAL across directories" and told you to hoist the counter to instance state and rewrite the `:466` refusal message and the `:117`/`:80-82` prose. Measurement at tip `1695a6afe` showed the counter is a per-directory LOCAL `$reads` TODAY (initialised at `:433`, checked at `:464`, refusal at `:466` says "this tier already reached its %d-file cap", prose says one tier directory at `:117` and `:80-82`), so that wording described a CHANGE under the words "restatement" — **the change is withdrawn.** Per-directory is the correct semantics here: the P6.S2 finding guarded against an untrusted clone forcing UNBOUNDED reads, and a per-directory ceiling over a fixed, closed directory set keeps the aggregate bounded while preventing rulebooks starving `rules/` of read slots; re-basing a P6.S2-reviewed cap test to a different semantics is not available work under §7. Do NOT hoist the counter to instance state; do NOT rewrite the `:466` refusal message or the `:80-82`/`:117` prose. Required test instead: a pack directory at the cap cannot lift the aggregate past (directories × `MAX_FILES`), the refusal names the directory that hit its own ceiling, and the loader doc-block states the aggregate arithmetic out loud.
9. **Prompt-effect assertion with zero golden movement:** two live renders under synthetic temp HOMEs (the `RuleLoaderTest` pattern; `SlashDispatchTest` uses `tests/Support/HomeSandboxTrait.php`, imported at `:18`/used at `:32`), asserting render(on) ≠ render(off) and that the `<user-rules>` fence and its authority preamble appear exactly once per enabled pack. No fixture edits.
10. **Census cascade, pre-authorized and expected.** The `.sugar-crush/rulebooks` literal obliges a `DOT_PATHS` row (`tests/Cli/ProjectTierRefusalInventoryTest.php:196`, 34 entries today), the distinct-literal assert at `:488` (**24→25 — MEASURE, do not assume**: the last two steps each shifted these figures by one on unrelated census grounds), this file's spelled prose at `:316-318`, the literal `'TWENTY-FOUR distinct'` at `:497`, and `Bootstrap::projectTierRefusals()`' doc-block (`src/Cli/Bootstrap.php:1264`, `:1267`). Classification, measured: the axis is *who chose the path*, so a `$HOME`-anchored literal is `self::USER` (like `Agents/Team.php|.sugar-crush/teams`), NOT `self::REPOSITORY` (which means the checkout under analysis points there — the sibling `rules` row earns that only for the project tier). Therefore repository-chosen stays at the measured **14** (`:489`, `:404`). But the new row leaves `$ownWords` (tops at 34, `:503`) and the distinct count leaves `$pathWords` (tops at 24, `:504`) — both maps must grow. If you classify it REPOSITORY anyway, `repositoryChosenPaths()` moves to 15 AND `$repositoryOccurrences` moves 17→18, overflowing `$repoWords` (`:505`, `:518`); in that case rulebooks joins the gap column as a **NEW GAP (6→7)**, not a drained feeder — say which you did. Feeders/gaps assert 8/6 today (`:589-590`); do not silently move the tuple.
11. **No new `error_log`/STDERR sink** — reuse `RuleLoader::report()` (`:579-584`), the `SUGARCRUSH_DEBUG_RULES` channel (`:203`) already rostered at `docs/ENVIRONMENT.md:52` and `tests/Cli/StderrEmitterCensusTest.php:295`. No new `SUGARCRUSH_*` env var, else `EnvRosterDriftTest` and `docs/ENVIRONMENT.md` move.
12. The new test file must NOT tree-walk (`HomeSandboxTrait`, temp dirs under `sys_get_temp_dir()`), must NOT clone a private helper name from a sibling file (`DuplicatedTestHelperDriftTest` reds on names ≤1 token apart), and must remember that every new `try` block costs 3 assertions in `AssertionSwallowingCatchTest` — a derived figure, so state the count in your return rather than asserting it is stable.

## GOLDEN REALITY

`tests/fixtures/prompt/golden-system-prompt.txt` = `f09f37366a1925565dcc7725f659ff41` / 7,829 B.
`tests/fixtures/prompt/golden-agent-prompt.txt` = `ef0326dd38535aaa2f1d715919bff26e` / 1,060 B.
**Both must stay byte-identical.** The REGEN LAW (`prompt_plan.md`, cited as **do-NOT-invoke** in `prompt_kit/briefs/P6.S2b-step-brief.md:79`) is not invoked here: there is no regen in P6.S3 and a golden move is itself the defect. Leave the 7,829-byte pin at `tests/BaseSystemPromptTest.php:835` alone, and add no fixture rulebooks — `ensureFixtureUserHome()` (`:1747`) checks only the `…/rules/global-style.md` sentinel at `:1751`, so new fixture files would not even be delivered on a warm `vendor/` tree, and user-tier content DOES render into the golden.
Verification: `git diff <base>..HEAD -- sugar-crush/tests/fixtures/` **must be empty**.

## Prediction first (law)

Write the expected headline figures — assertions, tests, files, the full-suite tail — down BEFORE launching any suite, then reconcile every mover to zero remainder with `python3 prompt_kit/tools/cmp.py <branch-junit> <master-junit>` (branch FIRST). Name the tree each figure came from: a measurement can be provably redundant, and an unreconstructible number is worse than none.

## Method

Land the loader change first (rulebooks dir, per-directory counter — semantics UNCHANGED, NIT-5) and get `RuleLoaderTest` green in isolation; then the command trio (registry row, helper class, Chat arm); then the state ride through App/Runtime/EngineBackend; then the tests of requirements 4-9; the census cascade last, because its figures are only knowable once the literal exists.

## Review cycle

Builder → reviewer → fix loop until clean, one cycle minimum. The reviewer scores hard requirements by number; every unmet number is a finding. Do not merge with a finding open, and do not let a reviewer talk you into a golden regen.

## Return format (this becomes the worklog; omit nothing)

1. One-line goal restatement + the exact file list you touched.
2. Design decisions with reasoning: the disabled-set representation, the pack-identity key, the AND-semantics evaluation point, the per-directory counter's aggregate arithmetic.
3. The six §1.11 deletion experiments: exact commands + both outputs for each.
4. Every measurement: solo figures, all guards, the **before/after census table with old→new** for each figure, goldens before/after, your written prediction vs the actual full-suite tail verbatim, and per-class reconciliation of any delta.
5. The two live-render assertions with their byte counts, and the `config.json` byte-identical proof.
6. Any premise in this brief that proved FALSE at your tip, with `file:line` proof — do not silently work around a wrong brief claim.
7. §1.10 escalations verbatim (or "none"), anything left dormant and why, the symlink refusal record, and confirmation that both goldens are byte-identical.
8. All commit shas with author/committer verification output.
