# P5.S4 Step Brief — §10.7: the verify-before-done clause

## 0. Mission
You are the step LEAD for P5.S4 of the sugar-crush prompt-architecture plan. Work ONLY in
/home/sites/prompt-step-P5.S4 (branch prompt/P5.S4, based on master 2d3a096d5). Read every reference
below BEFORE editing anything.

## 1. Required reading, in order
1. /home/sites/prompt-scratch/P5.S4/step-text.md — the step text, verbatim. Its "Done when" is your acceptance bar. (Its line citations are PRE-refactor; the corrected anchors are §3 below — trust §3.)
2. /home/sites/sugarcraft/prompt_plan.md §1.10, §1.11, §16, §17.
3. /home/sites/sugarcraft/prompt_expand.md §9.6, §4.25 points 1-2, §2.3 — the dossier sections this step names.
4. sugar-crush/src/Runtime.php:2631-2722 — basePrompt() + the enforcement-citation bar comment.
5. sugar-crush/tests/BaseSystemPromptTest.php:1-360 — structure tests, BASE_END_MARKER, allowlist.

## 2. Figures addendum (NEW FLOOR — everything pre-sync is historical)
- Floor full suite: Tests 10774 / Assertions 165986 / 0E / 0F / Skipped 2 / EXIT 0, measured at 9f3348ef5 on this exact tree (tip 2d3a096d5 differs by bookkeeping files only).
- MouseModalGuardTest arms itself by viewport at ±3 assertions (198/201). NEVER adjudicate a ±3 headline delta by headline; per-class junit diff: python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py <branch.xml> <master.xml>.
- goldens (md5, PRE-STEP): system 32ea749d84938811ac9331419cae7380 · agent ef0326dd38535aaa2f1d715919bff26e. The SYSTEM golden MOVES in this step legitimately - that is the deliverable. The AGENT golden must stay UNMOVED (verified 2026-09-05: the Agent path does not carry the base heredoc).
- BOOKKEEPING CORRECTION (post-step, 2026-09-05, measured at master 9a5197065): the step DELIVERED - system golden moved as designed to 8c41b8f0a9573974897d6b3a9d7ab9f2 (5,559 B; byte pin 5,176 -> 5,559); agent golden UNMOVED at ef0326dd38535aaa2f1d715919bff26e. The orchestrator step-close summary quoted the new system md5 with a 57->75 digit transposition (…a975… for …a957…); this line carries the disk md5sum, verified via md5sum sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt.
- sugar-crush/vendor/sugarcraft/* are packagist REAL dirs now. Editing monorepo lib dirs does NOT reach sugar-crush tests. Never run composer.

## 3. Re-derived anchors (post-P5.S1 refactor + post-sync line numbers — the step text's citations are stale)
- Base heredoc: Runtime::basePrompt(), sugar-crush/src/Runtime.php:2636-2722. The clause must land INSIDE this heredoc, BEFORE its closing line (the marker below).
- Enforcement-citation bar comment: Runtime.php:2638-2675.
- tests/BaseSystemPromptTest.php: REQUIRED_SECTIONS const :49-54 (the four-heading pin); heading-level test :239-254; order+non-empty-body test :261 onward; capitalised-word allowlist :295-348; BASE_END_MARKER = 'commands to follow.' at :105 - the base slice ends at that sentence, and its uniqueness-ahead-of-env is asserted (:145-152). A clause added after the marker sentence is OUTSIDE the policed base slice - put it inside, or deliberately move the marker and update its docblock.
- Golden pin: testSystemPromptMatchesCommittedGolden() :646, assertSame :671-675. Regeneration discipline docblock :634-644: NO env hook exists. The valid procedure: an ad-hoc /tmp PHP script that reflects exactly what the test does (goldenContext() + buildSystemPrompt()) and passes the render through pinHostLines() before writing the golden. A raw dump is NOT a valid golden.

## 4. What to build
Add the verify-before-done clause to the base prompt per step-text.md. DECIDE AND RECORD BEFORE EDITING:
(a) Fifth heading vs fold-into-existing. If a fifth heading: deliberately widen REQUIRED_SECTIONS and write the WHY into the test's docblock (the step text forbids discovering this mid-edit).
(b) Every clause must clear the :2638-2675 bar: name the code that makes it true AND the limit past which it stops being true. Pure-behavioural guidance that has no enforcing code must be justified in a docblock and kept truthfully bounded (e.g. how a runner is discoverable in THIS app - the Bash tool; what verification the app itself does and does not do).
(c) Register: no stacked IMPORTANT:/CRITICAL:/You MUST. Plain statement + reason.
(d) Any capitalised word your prose introduces must already be in the :295-348 allowlist - prefer prose that adds none; widening the list is a deliberate documented act.

## 5. Tests (acceptance, per §1.11 - real calls + values, red-on-revert)
- A structural test proving the clause renders in the base slice (whole-line regex if a heading; exact-anchor phrase if folded), passing it through the existing qualifierBefore()/assertPromptDoesNotNegate() polarity machinery where applicable.
- Golden regenerated; the golden test green against the NEW golden; the OLD-vs-NEW golden diff pasted VERBATIM into your report (the worklog copy is the orchestrator's job).
- Red-on-revert experiment: remove only the clause -> your new test goes red; restore.
- Do NOT weaken/rename/delete any existing assertion. Allowed edits to existing tests: the documented REQUIRED_SECTIONS widen and/or allowlist widen, each with its reason.

## 6. Process laws
- Solo test runs while iterating (tests/BaseSystemPromptTest.php, tests/RuntimeTest.php) are fine. Final: ONE full suite at the branch tip: cwd = /home/sites/prompt-step-P5.S4, php vendor/bin/phpunit -c sugar-crush/phpunit.xml style invocation from checkout root, serial, </dev/null, --log-junit /home/sites/prompt-scratch/P5.S4/lead/junit-branch.xml. Box-quiet probe FIRST, standalone (never in the same command as the suite): ps -eo cmd | /usr/bin/grep -c '^php .*phpunit' must print 0. Suite takes ~7 minutes - run it in-session and wait on it; do not return with the run pending.
- Write your prediction (test/assertion deltas per class) BEFORE the final run; report prediction vs actual.
- Commits: use git commit -F <file> under your scratch dir; ZERO '<' or '>' characters in any commit message (grep -c '[<>]' must be 0 before use - subjects feed a fixture log window). Identity comes from the worktree's LOCAL git config (already set to the repo owner); NEVER type an email address into any file or command - if a tool shows a redacted placeholder token, that is why. Byte-verify each commit object: git cat-file commit HEAD | /usr/bin/grep -c '\[EMAIL\]' must be 0.
- Scratchpad: /home/sites/prompt-scratch/P5.S4/lead/ is yours ALONE. No git init anywhere else; verify pwd before any git command. Never rm -rf a path you did not create.
- Lead never fixes: you implement, self-verify, report. Fix cycles are orchestrator-run with separate agents.
- §1.10: if the only way you see forward involves removing or stubbing existing code/tests, STOP and record an escalation instead. Removal is not an available outcome.

## 7. Deliverables
1. Branch prompt/P5.S4 pushed to nothing - local commits only, message format per prompt_plan.md §1.6.
2. /home/sites/prompt-scratch/P5.S4/lead/report.md (<=150 lines): what you built, the heading-count decision + reason, the full golden diff, predicted vs actual suite figures, red-on-revert evidence, residuals/follow-ups.
3. junit-branch.xml at the path above.
