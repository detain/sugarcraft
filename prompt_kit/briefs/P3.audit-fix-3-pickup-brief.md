# P3.audit-fix-3 — PICKUP brief (new lead; previous lead cancelled mid-cycle-3 fix)

## Who you are
You are the NEW step lead for P3.audit-fix-3. The previous agent completed 8 commits and
three review cycles, then was cancelled by the user (context size) while mid-way through
fixing the cycle-3 findings. Do NOT resume its session, do NOT redo its history. You pick
up from disk.

## Read first (in this order)
1. /home/sites/sugarcraft/prompt_kit/briefs/P3.audit-fix-3-step-brief.md — the original
   step brief. EVERYTHING in it remains authoritative (sandbox, golden constraint,
   declared files, figures/suite rules, prohibitions, report format) EXCEPT its
   "Your review loop" section, superseded by this brief.
2. /home/sites/prompt-scratch/P3.audit-fix-3/review-3/findings-cycle-3.md — the five
   findings (F-1..F-5) the in-flight work closes.
3. /home/sites/prompt_plan.md sections: 1.4 (line 366), 1.8 (609), 1.10 (827), 1.11 (874),
   16 (2776+), 17 (3274+). Read from disk; line numbers re-derive with grep if stale.

## State on disk (orchestrator-measured 2026-09-02)
- Worktree /home/sites/prompt-step-P3.audit-fix-3, branch prompt/P3.audit-fix-3, 8 commits
  over master@3634aa1cb: 80233c172 F6 | f49dd1420 F1 | 1ed87fdc5 F5 | dae96307f F2 |
  4860997da F4 | 411c49179 cyc-1 (defeats 14-15) | 727aee03e cyc-1 (class_alias reader,
  unpinned imports) | 2a9381667 cyc-2 (defeats 16-18, label pairing, full-name keys,
  runtime-map kind split).
- UNCOMMITTED (the inherited WIP): sugar-crush/tests/RuntimeTest.php ~+470 and
  tests/TreeWideGuardRosterTest.php ~+227 (607 insertions / 90 deletions total).
- From the dead lead's transcript tail: it had implemented the keyword-arm rewrite (trait
  user + anon-name resolution), the enumeration/cannot-express updates (trait split,
  heredoc, const DECLINED-by-name), extended the probe with cycle-3 rows, added
  blind-spot-table rows for declined shapes, added direct-drive tests for the literal
  readers, and its K12-K16 deletion experiments reported "all six cycle-3 arms proven
  load-bearing". Last visible action: updating the roster closure prose. It had NOT run
  or committed the cycle-3 result.

## What you do, in order
1. AUDIT the uncommitted WIP: git diff, file by file, against findings F-1..F-5. Each
   finding must have a closing arm (detect) or an honest declared-with-pin row (declare).
   Silence is not an outcome. Fix ONLY what is inconsistent or missing in the WIP itself —
   that completion is your one allowed direct edit; all FUTURE findings go through a fix
   agent (step 6).
2. COMMIT the WIP: check identity first (git config user.email = detain@interserver.net),
   write the message to a file, commit with -F. Suggested: "prompt: P3.audit-fix-3 fix
   cycle-3 - close F-1..F-5 on scanner and roster (inherited WIP, verified by pickup
   lead)". Amend/extend freely as your step-1 completion lands.
3. VERIFY: full suite per the step-brief suite rules (cwd = worktree root, serial,
   </dev/null, box-quiet probe prints 0, prediction in
   /home/sites/prompt-scratch/P3.audit-fix-3/pickup/prediction.md BEFORE the run,
   reconcile movement with /home/sites/sugarcraft/prompt_kit/tools/cmp.py). Re-run every
   deletion experiment the dead lead claimed (K12-K16) yourself - its transcript claim is
   not evidence; paste your RED outputs. Census: nine-file subset + TreeWideGuardRoster
   Test alone; record the NEW derived roster figures honestly (they moved with added rows).
   Goldens must be byte-identical (md5 32ea749d... / ef0326dd...); if any byte moved, run
   16.2 and flag prominently in the report.
4. SPAWN CYCLE-4 REVIEWER: fresh task(coder) subagent, READ-ONLY mandate (no edits,
   commits, or config; experiments only with restore; own subdir review-4/). Brief it
   LEAN: point at this brief, the step brief, the findings files of cycles 1-3, diff
   range master..HEAD, the 19-check list at 1.4, and demand deletion experiments on every
   arm this step added. It writes findings to
   /home/sites/prompt-scratch/P3.audit-fix-3/review-4/findings-cycle-4.md AS IT FINDS
   THEM, and its final message is <= 100 dense lines.
5. If cycle 4 returns NO findings and its report accounts for every check: proceed to
   step 7.
6. PROCESS RULE (new, non-negotiable - supersedes the step brief's loop): YOU NEVER APPLY
   REVIEW FINDINGS YOURSELF. For each cycle with findings: spawn a FIX agent via
   task(coder) with a short brief - findings-file path, sandbox path, step-brief path,
   its own subdir fix-N/, the commit-early/golden/identity/deletion-experiment rules, and
   the same context discipline clause you are under. Wait, then VERIFY its commits with
   your own measurements (focused runs + full suite) before spawning the NEXT fresh
   reviewer. Cycles continue while each still finds real defects; hard cap cycle 6 total.
   If cycle 6 still finds findings, stop and report full state to the orchestrator - do
   not declare clean, do not grind silently.
7. FINAL REPORT (your only deliverable; <= 150 lines, dense, tables not prose): 7 sections
   per the step brief, plus (a) your WIP verdict - what you changed in step 1 before
   commit, (b) cycle ledger: every cycle, reviewer, finding count, disposition, who fixed
   (fix-agent for cycles 4+), (c) the re-run deletion experiments' RED output lines,
   (d) new roster/census figures old-vs-new, (e) any 1.10 escalation verbatim.

## Context discipline (why the last lead died - do not repeat it)
- Keep tool outputs small: tail/grep them, redirect long logs to your scratchpad, read
  slices. Never cat a suite log whole. Batch edits; no exploratory wandering.
- Intermediate messages 1-2 lines, no narration; strictly sequential tool use; subagents
  are never polled - task() returns their full report.
- If a subagent returns blank/aborted/truncated, it DIED: 1.8 ladder - respawn fresh in
  the same worktree with a continuation instruction ("do not start over"). A blank return
  is NEVER "no findings".

## Prohibitions (standing)
Never push; never caliber; never --no-verify; never composer install/update; never modify
git config; never weaken/skip/rename/delete an existing test; never remove dormant code;
/usr/bin/grep for tree scans; never touch crush-lane-* or docs/plans/crush_code_*; your
scratchpad is /home/sites/prompt-scratch/P3.audit-fix-3/pickup/ (rm -rf only inside your
own subdirs).
