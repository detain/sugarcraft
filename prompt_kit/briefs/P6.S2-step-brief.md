# P6.S2 — `RuleLoader`: three tiers, containment with refusals RECORDED, and the user-tier framing (step brief)

**Plan reference:** `prompt_plan.md` Phase 6, P6.S2 (plan lines 2084-2115). **Sources:** `prompt_expand.md` §9.13, §2.5, §2.13, §10 seam 8. **Depends on:** P6.S1, merged `88fa18f77`. **Split ruling 2026-09-05:** D3 moved OUT of this step into new step **P6.S2b** — read "NOT in this step" before you plan anything.
**Read before coding:** `prompt_plan.md` §1.10, §1.11, §16, §17, this repo's `AGENTS.md` conventions, and `prompt_kit/briefs/P6.S1-step-brief.md`. §1.11 test substance, the house idiom, the commit-and-identity law, ORCHESTRATION-RULE-2 and -3, the box-quiet probe, the `MouseModalGuardTest` 198-vs-201 wobble, the suite floor **10832 / 166702 / Skipped 2 at `88fa18f77`**, prediction-before-run and the reviewer→fix→**NEW**-reviewer cycle all carry over from that brief **by reference** — they are not re-derived here.

## Step text (verbatim from the plan)

> **Goal** A loader mirroring the tiering `InstructionFileLoader` and `CommandLoader` already implement:
> 1. `~/.sugar-crush/rules/*.md` — user-global
> 2. `<root>/.sugar-crush/rules/*.md` — project, shipped in-repo
> 3. `<root>/RULES.md` — optional single-file root rules
>
> Ordered by filename. Frontmatter: `name`, `description`, `enabled`, `models`, plus the trigger keys from P6.S1 (`paths:`, `keywords:`).
> **Hard constraints**
> - Every read gated by `ContainedPath::within()`, the way `InstructionFileLoader` and `CommandLoader` already are. `CommandLoader`'s own comment states the threat: *"cannot smuggle in ~/.ssh/config as a prompt."*
> - Refusals are **recorded** in a `refusedPaths` list, not silently skipped. A silently skipped read is indistinguishable from an empty file.
> - De-duplicate on `realpath()`, and case-insensitively — upstream `crush` dedupes twice (sort+compact, then a lowercased map key) precisely because `rules.md` / `Rules.md` / `RULES.md` all load on a case-insensitive filesystem.
> - Depth cap and file-count cap, both with a test at the cap.
> **Done when** the containment test symlinks a rules dir at `$HOME/.ssh` and asserts refusal, and a separate test asserts the refusal is *recorded*.

## Declared file list

- `sugar-crush/src/Context/RuleLoader.php` (new)
- `sugar-crush/src/Context/Rule.php` (new)
- `sugar-crush/tests/Context/RuleLoaderTest.php` (new)
- `sugar-crush/tests/Context/RuleLoaderContainmentTest.php` (new)

**Plus the pre-authorized ledger cascade, which the plan does not declare but which this change forces** (this is finding **F-LEDGER**; editing these is in scope, editing anything else is not): `tests/Support/ReadPathCensusTest.php`, `tests/Support/ContainedPathInventoryTest.php`, `src/Support/ContainedPath.php` (its prose number-words), `tests/Cli/ProjectTierRefusalInventoryTest.php`, and the `Bootstrap::projectTierRefusals()` docblock in `src/Cli/Bootstrap.php`. If you touch the `DuplicatedTestHelperDriftTest` or `EnvRosterDriftTest` ledgers, those are in scope too.

## Consumes P6.S1 — read these four first

`sugar-crush/src/Context/Triggers/` ships `Trigger` (zero-method union marker), `KeywordTrigger` (`::new(array $words)`, `matches`/`matchedWords`/`fires`/`firedWords`/`mergeFiredFrom`), `PathTrigger` (`::new(array $globs)`, `matches`/`matchingGlobs`, its own glob→PCRE compiler, documented **"A MATCHER, NOT A GATEKEEPER"** — containment is named as *this* step's job), and `IntentTrigger` (`::new(string $description, int $maxChars)`, `truncated`/`isTruncated`). They were shipped **unwired precisely so this step is their first consumer**: `paths:` → `PathTrigger`, `keywords:` → `KeywordTrigger`, `description` → `IntentTrigger`. Construct them from parsed arrays; do not re-implement matching, and do not modify them except for the **F-PATHDIALECT** reconciliation against `SkillRegistry::pathMatches()` (`src/Skills/SkillRegistry.php:561`) / `legacyPathMatch()` (`:628`), which that class's own docblock declares UNVERIFIED.

## NOT in this step (hard scope boundary)

1. **D3 — the `<harness-injected>` fence — is P6.S2b, not this step.** So is the `PromptFence` escape-roster widening (5→6/7 tags at `src/Context/PromptFence.php:70-76`) and the layer-mapping/nesting decision. **Do not touch `src/Context/PromptFence.php`.** Two fence gauntlets in one step double the golden work and muddy this step's pure-insertion audit trail.
2. Do not rewire `Skill::matchesPrompt()` — P7.S4 owns that.
3. Rulebooks and the `/rules` command are P6.S3; config keys are P6.S4; token caps are P6.S5.

## D1 and D2 ARE this step (the un-defer condition is satisfied)

`Runtime.php:2499-2502` merges `loadRoot()` and `loadForced()` into **one flat docs list** and gives every doc the **same** `<project-instructions>` framing — so user-chosen forced content currently speaks with project authority. The `<user-rules>` fence's feeder tier is exactly what this step builds, which is the condition Phase 5 set for shipping it.

- **D2** — give the user tier its own framing, paralleling `Runtime::INSTRUCTIONS_AUTHORITY_PREAMBLE` (`src/Runtime.php:103`): state provenance and authority in prose, name no fence tag in the preamble text, keep it ASCII.
- **D1** — the `<user-rules>` fence carrying it, spliced at the same single construction site so there is no bypass path.

## Rulings — already decided; do not re-litigate

- **OD1** The project rules tier **is allowed**. §2.12 governs **who chooses** the pattern, not **where files live**. `prompt_expand.md` seam 8's "user-tier-only" wording **contradicts the plan's own tier list**; resolve the contradiction in the class docblock, or the next agent under-builds.
- **OD2** Refusals are **loader-local**, in the `InstructionFileLoader::refusedPaths()` shape (`src/Context/InstructionFileLoader.php:762-765`, map property `:101`) — **not** a `Cli/Bootstrap.php` drain. Therefore `.sugar-crush/rules` enters `ProjectTierRefusalInventoryTest` as a declared **GAP** (gaps 5→6).
- **OD3** Dedup **pass 1** on `realpath()`, **pass 2** on `strtolower(realpath())`; load order user → project → root; first-seen wins; **both passes pinned by test**. The case-insensitive pass has **no precedent in this repo** and "upstream dedupes twice" was **not corroborable from this checkout** — cite it as **spec**, not as a mirror.

## Precedent reality (this corrects the plan's own framing)

- **`CommandLoader` is the ONLY user-tier template.** `loadUserCommands()` `src/Commands/CommandLoader.php:433-449` (uses `HomeDirectory::owned()` at `:435`), `loadFromDirectory()` `:327-402`, `MAX_DEPTH = 4` at `:77`, `ContainedPath::below()` at `:334`, per-entry `ContainedPath::within()` at `:375`, and the repo's **only** sort precedent `ksort($commands)` at `:399`.
- **`InstructionFileLoader` implements NO user tier and actively refuses `$HOME`** (`:353-355`, policy docblock `:296-315`). Clone its containment / refusal-recording / realpath-dedup **discipline**, not its tiers.
- **Three requirements are net-new — no precedent exists in this repo:** the file-count cap; the "list a directory's `*.md` files and order them by filename" loop; and the case-insensitive dedup pass. Write them from spec and say so in the docblocks.

## Mandatory API choice

Use `HomeDirectory::owned()` (`src/Support/HomeDirectory.php:199-235`), which returns `?string` and yields null when ownership cannot be established. **NEVER `HomeDirectory::path()`** (`:131-134`) — it silently falls back to `sys_get_temp_dir()`, and `HomeDirectoryPathReaderInventoryTest` will then red for the wrong reason.

## GUARD TRIPWIRES — all DERIVED ledgers; do NOT read any of them whole

Run each solo and read only the failing assertion.

1. **`ReadPathCensusTest` `READ_PATHS`** (62 rows / 88 verdicts today) — needs one row per read sink you use, cross-linked to the next ledger so both move together.
2. **`ContainedPathInventoryTest` `ROUTED_CALL_SITES`** (34 calls / 14 files today) **plus** the spelled-out number-words at `src/Support/ContainedPath.php:97`. The hand `$words` map covers ELEVEN..EIGHTEEN and TWENTY-SEVEN..THIRTY-EIGHT, so **it OVERFLOWS at sum 39 — expect to extend that map.** That is a legitimate, pre-authorized test edit.
3. **`ProjectTierRefusalInventoryTest`** — `DOT_PATHS` 33 occurrences / 23 distinct / 13 repo-chosen today; `$ownWords` ceiling 33, `$pathWords` ceiling 23, the repo-chosen count pinned at 13; the holders wired/dormant split needs a `RuleLoader` row whose driver is `RuleLoaderContainmentTest`; and the feeders-vs-gaps union (see OD2).
4. **`Bootstrap::projectTierRefusals()` docblock prose** — the strings "THIRTEEN repository-chosen" and "TWENTY-THREE distinct" are pinned.
5. **`DuplicatedTestHelperDriftTest`** — give the two new sibling test files **distinct private helper names**; do not copy helpers from `CommandLoaderContainmentTest` or `InstructionFileLoaderContainmentTest`.
6. **`EnvRosterDriftTest`** — if you clone `CommandLoader`'s `SUGARCRUSH_DEBUG_COMMANDS` idea as `SUGARCRUSH_DEBUG_RULES`, you must add the `docs/ENVIRONMENT.md` row. Reading `$HOME` itself does **not** count: the name pattern is `/^SUGAR_?CRUSH_[A-Z0-9_]+$/`.
7. **`SymbolCitationDriftTest`** polices **TEST-symbol citations only** (`:71-76`) — production names are safe to reference. **Write citations in the bare or FQN form, never `self::`,** which the guard silently ignores (finding **F-GUARD**).
8. **`TreeWideGuardRosterTest`** — do not make either new test tree-walking; a root-anchored `glob()`/`scandir()`/`RecursiveDirectoryIterator` auto-joins it to the roster.
9. **`SuiteSkipRoster`** — do not call `markTestSkipped`.

## GOLDEN REALITY — say it plainly to yourself before you start

An unwired `RuleLoader` is golden-clean. The moment D1's `<user-rules>` section joins the assembly this becomes a **golden-MOVING step**. Expect, and be ready to produce: system golden regen under the **REGEN LAW** (`ensureFixtureRepo` FIRST, then `pinHostLines`, through a throwaway `/tmp` script reflecting `goldenContext()` — there is **no CLI flag**), the `BaseSystemPromptTest:821` byte-pin (currently **7,314**), `tests/Context/PromptSectionTest.php:423`'s distinct-production-fences assertion, and `PromptStabilityTest`'s stable-layer membership/order pin plus `STABLE_LAYER_WIDTHS` / `STABLE_LAYERS_BYTES` / `MIN_STABLE_PREFIX_BYTES` / the across-turn offsets. **Every move must be justified as PURE INSERTION**, with the byte arithmetic shown.

## Hard requirements

1. Every tier's every read gated by `ContainedPath`; a refusal is **recorded** with a reason string, never silently skipped.
2. A test at **each** cap — depth AND file-count — each with a **just-under / just-over pair**.
3. The plan's done-when, both halves: the containment test symlinks a rules dir at `$HOME/.ssh` and asserts refusal (direct precedent `tests/Support/CommandLoaderContainmentTest.php:191-193`, where `.claude/commands` is actively refused), **plus a separate test asserting the refusal is RECORDED**, not merely skipped.
4. Consume the P6.S1 triggers from parsed frontmatter; pin load order and both dedup passes; every public method ≥1 test; `final`, `declare(strict_types=1)`, `::new()`, immutable `with*()` via private `mutate()`, bare accessors.
5. **§1.10** — removal is not an available outcome. If the only way forward is to delete or redesign something, STOP and report it as an escalation with `file:line`; that is a completed step.

## Method (this is why three agents died in the last worktree)

- **Reading budget ≈300 lines.** Do NOT read `ReadPathCensusTest.php` (844), `ProjectTierRefusalInventoryTest.php` (991), `SymbolCitationDriftTest.php` (~1,000), `DuplicatedTestHelperDriftTest.php` (2,458) or `TreeWideGuardRosterTest.php` (4,197) whole. Run them solo; read only the failing assertion.
- Commit **granularly** (src, tests, each ledger cascade as its own reviewable commit). Run the full suite **exactly ONCE, at the final tip after the last commit** — commit subjects are `<env>` fixture input, so a suite run before the last subject is a measurement of a different tree.
- **Two laws that bit this session, restated:** (1) a `/** … */` **docblock cannot contain a literal `**/` sequence** — it terminates the comment; document the glob dialect in prose with a parenthetical. (2) **A blank agent return means resume the SAME agent, up to ~10 times — never treat it as a result**, and never shrink the step to cope with it.
- Paste every measurement. Any premise in this brief that proves FALSE at your tip: report it with `file:line`; do not silently work around it.

## Review cycle

Same law as P6.S1: fresh read-only reviewer over the diff → fixer → **a brand-new reviewer**; break only on clean; cap five cycles. Blank/aborted/truncated = the agent DIED, never "no findings".

## Return format

The P6.S1 brief's eight-item return format, plus: the golden before/after pair with the pure-insertion arithmetic, every ledger row you moved with its old→new figures, and the OD1/OD2/OD3 implementations as `file:line`.
