# P6.S2b — the `harness-injected` roster channel, no emitter (step brief)

**Plan reference:** `prompt_plan.md` Phase 6, `### P6.S2b` (plan lines 2124-2201) — READ THE RULING FIRST, it is this step's spec, and it is newer than the step's own "OPEN" framing.
**Sources:** §3, §17.2 invariant 9, `prompt_expand.md` §9.15 / seam 8; premise check `binding-harlequin-wolf` (2026-09-05).
**Depends on:** P6.S2 (merged — the escape-roster widening was consumed there, which is why this step adds ONE tag and not two).
**Ruling date:** 2026-09-05, recorded in-plan at commit `60e79df53`.

**Read before coding:** `prompt_plan.md` §1.10, §1.11, §16, §17 and the whole `### P6.S2b` section; the P6.S1 and P6.S2 worklog entries at the top of `prompt_worklog.md`; the P6.S1 brief (its return format and method carry over by reference and are not restyled here).

## Step text (pointer, not a copy)

> RULING 2026-09-05: no layer is wrapped at this tip. `<harness-injected>` lands as the seventh
> `PromptFence::TAGS` entry with NO emitter — the `system-reminder` precedent
> (`tests/Context/PromptSectionTest.php:502-516`) proves a roster tag can be load-bearing for
> defang-only purposes. Done-when: a forged `</harness-injected>` inside a project-instruction
> document cannot render — a roster + guard property, provable with ZERO golden movement.

## Declared file list — exactly three

- `sugar-crush/src/Context/PromptFence.php`
- `sugar-crush/tests/Context/PromptSectionTest.php`
- `sugar-crush/tests/BaseSystemPromptTest.php`

The scope is DELIBERATELY small. Do NOT grow it. Do NOT touch `src/Context/Sections/MaximsSection.php` or
`tests/Context/Sections/MaximsSectionTest.php` — their do-not-widen notes are the ruling's *evidence*, not its
target, and both stand unchanged. Do NOT touch `src/Runtime.php`. Do NOT add any new `src/` file. Do NOT add a
`fence()`-returning section: no emitter is sanctioned by this ruling.

## Rulings — already decided; do not re-litigate

1. No wrapper today. Wrapping `repo-map`…skill-listing would put the unescaped skill bodies
   (`src/Skills/Skill.php:107-110` — no `PromptFence::escape()` anywhere in `src/Skills/`) under a
   harness-authorship frame the bytes cannot honour. Wrapping only `maxims` moves the golden for a
   provenance claim the two standing notes reject.
2. Nesting is mechanically unavailable: `escape()` is depth-blind (`src/Context/PromptFence.php:118`) and
   `PromptSection` is a 4-method leaf protocol. The wrap defers to the step that builds the real
   harness-voiced channel (§9.15) — a follow-up, not this step's obligation.

## Hard requirements

1. Add `'harness-injected'` to `PromptFence::TAGS` (:78-85). TWO files, TWO orders, ONE insert — pre-name both
   before editing: the DECLARATION-order tripwire `tests/BaseSystemPromptTest.php:1000-1004` compares
   `array_keys($expected)` against `tags()`, so the `$expected` map (:992-999) gains the key in exactly the
   position the const gains it; the SORTED literal at `tests/Context/PromptSectionTest.php:302-309` needs it in
   sorted position.
2. Rewrite the `TAGS` docblock tail (:67-74) so the pin population is stated truthfully. It says the widening
   is "pinned by BaseSystemPromptTest's user-rules forgery guard" (singular) when FIVE sites pin it: the two
   whole-roster pins plus the three tier forgery guards (`:1109` user, `:1231` project, `:1324` root). Do NOT
   write any spelled-out character/byte count or digit-plus-"characters" figure into that prose —
   `GlobFigureDriftTest` polices that alphabet, and P6.S2 deferred this very edit for exactly that reason. The
   census move this rewrite causes is expected; attribute it, do not dodge the edit to avoid it.
3. The guard this step OWES (no emitter, so the guard is the proof). Mirror
   `PromptSectionTest.php:502-516`: assert `PromptFence::escape()` neutralises BOTH `<harness-injected>` and
   `</harness-injected>` polarities. Then the plan's literal done-when: render a project-instruction document
   whose body contains a forged `</harness-injected>` through the PRODUCTION assembler and pin that it arrives
   escaped, in count form — `assertSame(N, substr_count(...))` — never a boolean
   `assertStringNotContainsString`. Keep it non-tree-walking (no `RecursiveDirectoryIterator`, no `__DIR__`
   scans) and pin any HOME-dependent render through the existing `renderUnderFixtureUserHome` helper
   (`tests/BaseSystemPromptTest.php:1665`). P6.S2's MINOR-2 lesson: `PromptSectionTest.php:423` went
   non-hermetic through `Runtime.php`'s ambient-`$HOME` fallback — do not re-create that.
4. Tripwires that fire BY DESIGN — pre-named so nobody panics or weakens one: the sorted pin (:302-309), the
   declaration-order tripwire (:1000-1004), the `$forgedDoc` payload and its "thirteen spellings / twelve
   polarities" prose (`BaseSystemPromptTest.php:975-991`), the "(six since the P6.S2 fix…" prose (:931), and
   the derived source censuses. The per-tag opener-pin literals at `PromptSectionTest.php:325-330` do NOT fire
   (additive-safe) but silently UNDER-cover tag 7 until a line is added — add the line.
5. §1.11 bar — state the deletion experiment up front, then RUN and PASTE it: removing `'harness-injected'`
   from `TAGS` must redden the new guard AND both roster pins; deleting the `escape()` call at the
   project-instructions splice (`src/Runtime.php:2613`) must redden a test. P6.S2 cycle 2 found that splice
   unpinned and fixed it, so the second half should already hold — if it does NOT, report it as a finding, do
   not work around it. A comment, a `class_exists`, or an annotation is not a test.
6. §1.10 — nothing may be removed. If the seventh tag forces a change to an existing guard's *meaning* rather
   than its list, STOP and report it as an escalation. Do not narrow a guard.

## GOLDEN REALITY — zero movement, as a hard check

System golden MUST remain `f09f37366a1925565dcc7725f659ff41` at 7,829 B and agent golden
`ef0326dd38535aaa2f1d715919bff26e` at 1,060 B, and
`git diff <base>..HEAD -- sugar-crush/tests/fixtures/prompt/` must be EMPTY. There is NO regen in this step.
The REGEN LAW (`ensureFixtureRepo` FIRST, then `pinHostLines`) is cited only so you know it exists and know NOT
to invoke it here. Any golden byte moved in P6.S2b is a defect to be reverted, not a number to be re-pinned.
Derived-ledger growth that IS expected: `GlobFigureDriftTest` (requirement 2's docblock rewrite) and
`AssertionSwallowingCatchTest` (+3 per new `try` block). There are no new `src/` files, so
`BinSugarcrushWiringTest` must NOT move — if it does, something outside scope changed.

## Process facts measured this session (all of these are load-bearing)

- Box-quiet probe: `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` (the bracketed-`v` form false-alarms).
- `MouseModalGuardTest` legitimately wobbles 198↔201 by viewport. Never "fix" it; reconcile any ±3 per class
  with `python3 prompt_kit/tools/cmp.py <a-junit> <b-junit>`, never by headline.
- Full-suite floor at `3a7094eab`: Tests 10866 / Assertions 167264 / Skipped 2 / EXIT 0.
- State a written prediction BEFORE running the suite; run long suites IN-SESSION and return only when every
  promised measurement is on disk.
- A `/** … */` docblock cannot contain a literal `**/`. Commit subjects are `<env>` fixture input — keep them
  tag-free. `{@see self::testFoo()}` is invisible to `SymbolCitationDriftTest` (F-GUARD blind spot): cite test
  symbols in the BARE form. No scratch `/tmp` path in any docblock (the worklog records those).
- Do not create a scratch git repo outside your own scratchpad; never `rm -rf` a path you did not create.
- `docs/ENVIRONMENT.md` is at `sugar-crush/docs/ENVIRONMENT.md`.

## Return format

The P6.S1 brief's eight items, plus: the golden before/after pair with the arithmetic proving it is UNMOVED;
every pin you moved with old → new figures; and the deletion-experiment output verbatim.
