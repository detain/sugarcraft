# P3.CLOSE-r3-fix — record corrections for the six cycle-3 close-review findings

## Role
You are a dedicated FIX agent. Cycle-3 of the Phase-3 close review found SIX defects, ALL record-side (brief/worklog/resume prose, one historical scope amendment, one doc-comment table row). Zero production-code and zero test-logic defects. You make exactly the six fixes below, verify with the two gates, and commit. You do NOT review, do NOT merge, do NOT push, do NOT decide anything beyond the six listed items.

## Sandbox and scratchpad
- Worktree: /home/sites/prompt-step-P3.CLOSE-r3-fix, branch prompt/P3.CLOSE-r3-fix, HEAD cf41aacd6 (vendor already hardlinked there — NEVER run composer install/update).
- Scratchpad (write ONLY here for temp files): /home/sites/prompt-scratch/P3.CLOSE-r3/fix-1/
- You edit these live files IN THE SANDBOX: prompt_kit/briefs/phase-review-brief-r3.md, prompt_kit/briefs/P3.audit-fix-3-step-brief.md, prompt_worklog.md, prompt_resume.md, sugar-crush/tests/RuntimeTest.php. NEVER touch /home/sites/sugarcraft paths directly for edits; the worktree is the only place you write (except your scratchpad and the VERBATIM-brief file above).
- Bash tool: ALWAYS pass command plus a large timeout when running suites (600000+ ms for the full suite); bash cwd RESETS between calls — absolute paths or chained cd everywhere. Use /usr/bin/grep (never bare grep) for tree-wide scans.

## Required reading before editing
1. Your six findings, six-field versions: /home/sites/prompt-scratch/P3.CLOSE-r3/review-1/findings-cycle-3.md
2. §1.11 of /home/sites/sugarcraft/prompt_plan.md (what counts as a test) and §16.8 rules 40/42/44 (corrections travel; three-part form; disclosure) — lines 827-896 and 3062-3256.
3. The current text around every site you touch (grep it, do not assume line numbers — they may have shifted since the reviewer read them).

## The six fixes
### F1 — brief HEAD sha
In sugar-crush-prompt phase-review-brief-r3.md (prompt_kit/briefs/phase-review-brief-r3.md), the sandbox section names HEAD 99227d29c. The tip that actually carries the merged work plus its bookkeeping is cf41aacd6 with ZERO sugar-crush diff between them. Update the named sha to cf41aacd6 and add a parenthetical: bookkeeping-only commits may move the tip above the merge sha; sugar-crush content identical.

### F2 — nine-file census figure correction
The orchestrator MEASURED at cf41aacd6, cwd = the checkout's sugar-crush dir, serial, stdin from /dev/null: OK (176 tests, 31255 assertions) for exactly these nine files: tests/SymbolCitationDriftTest.php tests/SwallowingCatchCensusTest.php tests/Support/DuplicatedTestHelperDriftTest.php tests/Support/ChildWallClockBudgetTest.php tests/Config/EnvRosterDriftTest.php tests/Tools/BuiltInToolCorpusTest.php tests/Support/InterpolationOpenerTokenTest.php tests/Support/ChildStderrCaptureTest.php tests/Config/GlobFigureDriftTest.php
The claim that this nine-file set measures 320 tests / 29,926 (or 29,918) assertions is UNREPRODUCIBLE — it comes from a pickup verification file with no file list; the 320-count set was never the nine.
Fix EVERY occurrence in the live records only — prompt_kit/briefs/phase-review-brief-r3.md, prompt_worklog.md (inside the P3.audit-fix-3 entry), prompt_resume.md (§4): replace the nine-file claim with OK (176 tests, 31255 assertions) at cf41aacd6, and where a since-then progression is appropriate note the same set measured OK (176, 31245) at 470e43569. Run /usr/bin/grep -rn for 320 and 29926 and 29,926 and 29918 across /home/sites/prompt-step-P3.CLOSE-r3-fix/prompt_kit/briefs/ /home/sites/prompt-step-P3.CLOSE-r3-fix/prompt_worklog.md /home/sites/prompt-step-P3.CLOSE-r3-fix/prompt_resume.md — fix live records; DO NOT rewrite any file under prompt_kit/findings/ or any prompt_kit/briefs/*pickup* file (those are historical records; leave them exactly as written).
Then add ONE bullet to the prompt_resume.md section-4 corrections list (after the existing bullets): the 320/2992x nine-file figure was an orchestrator error propagated from a pickup verification note with no file list; corrected 2026-09-02 by direct measurement to 176/31255; lesson — a census figure must always be accompanied by the file list it was measured over.

### F3 — audit-fix-3 scope amendment (widened by one file)
The merged P3.audit-fix-3 touched ten files: its nine declared plus src/Cli/Bootstrap.php (+1 line passing the unified project root as environmentRoot at the Agent assembler call site — required by the F5 seam fix; missed from the declared list because the pickup fix-lane re-declaration claimed the step's declared-file list stood).
Append to prompt_kit/briefs/P3.audit-fix-3-step-brief.md a clearly-marked section: SCOPE AMENDMENT (recorded post-merge by close-review cycle 3, 2026-09-02) in three-part form — what changed (src/Cli/Bootstrap.php +1 line environmentRoot), why it was necessary (F5 required the resolved root to reach Agent.php; Bootstrap is the call site), where it was recorded (close-review r3 finding F3; fix lane disclosure at prompt-scratch/P3.audit-fix-3/pickup/fix-agent-brief-template.md lines 12-13).
Correct every live-record line that says the step touched exactly 9 declared files (grep /usr/bin/grep -rn for "exactly 9" and "9 files" and "nine declared" in prompt_worklog.md and prompt_resume.md within the audit-fix-3 entry context) to: nine declared files plus one disclosed widening (src/Cli/Bootstrap.php, +1 line). Keep the distinction honest: the widening was real work on the F5 fix, not a violation of §1.10 — no dormant code removed.

### F4 — merge-table row description
In prompt_kit/briefs/phase-review-brief-r3.md merge table, the P3.S4 row f2af06eaa describes it as prefix-win ordering in tool-result truncation. That is false: S4 MEASURED the prefix win (3,095 visible-first ordering versus 4,670, zero production change — see the f2af06eaa commit message; and src/Runtime.php near line 661 says the tool-result path is NOT a prefix win). Rewrite the row: measure-the-prefix-win, zero production change.

### F5 — scanner blind-spot table: one missing silence row
In sugar-crush/tests/RuntimeTest.php the write-primitive scanner test file carries a blind-spot / what-this-alphabet-cannot-express table whose header contract is one row per silence. It has no row for a cross-file literal class_alias call (class_alias from a literal name to a write primitive declared in a DIFFERENT file): the target class name is never lexically visible to the file-local walk, scan returns the empty array, fail-open by mechanism.
First locate the table (search RuntimeTest.php for the cannot-express / blind-spot comment block) and CONFIRM TreeWideGuardRosterTest.php already carries the sibling row (grep it for cross-file and class_alias). Add the missing row mirroring that sibling's wording: cross-file literal class_alias — out of lexical reach; mechanism subsumed by the two-walks sentence; zero live population; count in src verified by you with: /usr/bin/grep -rn class_alias /home/sites/prompt-step-P3.CLOSE-r3-fix/sugar-crush/src | wc -l  (expected 0) — if nonzero, STOP and report instead of adding the row.
DOC COMMENT ONLY: no executable token changes. Check before editing whether SymbolCitationDriftTest or any other census test cites line numbers INSIDE sugar-crush/tests/RuntimeTest.php (grep the nine files above for RuntimeTest with line numbers like :[0-9]); if a file uses line-number citations into RuntimeTest.php, place your addition at the very END of the existing table block so no other line shifts — and re-verify the citing test file stays green afterward by running just that file.

### F6 — WorkflowEngine citation rot
The render sites that were :1252/:1294 in the audit-fix-3 era are at fresh line numbers in src/Workflows/WorkflowEngine.php at cf41aacd6 (the +23/+24 shift from the environmentRoot wiring). RE-DERIVE them yourself: /usr/bin/grep -n render /home/sites/prompt-step-P3.CLOSE-r3-fix/sugar-crush/src/Workflows/WorkflowEngine.php (the two double-render sites are the ones inside executeVerificationStage). Then update: phase-review-brief-r3.md (search for 1252 and 1294), prompt_resume.md (§3 item about the write signal and any §8 mention), prompt_worklog.md (audit-fix-3 entry mentions), and add one sentence to the phase-review-brief-r3.md text where the citation lives: cite call sites by function name plus line number because bare line numbers rot.

## Hard rules
- Goldens must not move: md5sum sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt must be 32ea749d84938811ac9331419cae7380 and golden-agent-prompt.txt ef0326dd38535aaa2f1d715919bff26e — verify BEFORE and AFTER your edits, report both.
- Do not weaken, skip, rename or delete any test. Do not change any executable token anywhere (the F5 edit is a doc-comment addition only).
- Do not touch /home/sites/crush-lane-a|b|c, docs/plans, sugar-crush/.sugar-crush-build, or anything outside the five live files listed above (plus your scratchpad and the VERBATIM brief file).
- NEVER push. NEVER git init/config outside your scratchpad. Do not merge to master.
- Commit in the SANDBOX with granular commits, one per finding plus one for the corrections bullet: git add <paths> && git commit -F <your-scratchpad>/msgN.txt with subject lines like P3.CLOSE-r3-fix: F2 correct nine-file census figure. Verify author on every commit: git log -1 --format='%an <%ae>' must be Joe Huss <[EMAIL]> — if not, do not commit, report it.
- Before ANY suite run: ps -eo cmd | /usr/bin/grep -c '^php .*phpunit' must print 0.

## Gates (run in the sandbox, after all six fixes and commits)
1. Nine-file census: cd /home/sites/prompt-step-P3.CLOSE-r3-fix/sugar-crush && vendor/bin/phpunit -c phpunit.xml tests/SymbolCitationDriftTest.php tests/SwallowingCatchCensusTest.php tests/Support/DuplicatedTestHelperDriftTest.php tests/Support/ChildWallClockBudgetTest.php tests/Config/EnvRosterDriftTest.php tests/Tools/BuiltInToolCorpusTest.php tests/Support/InterpolationOpenerTokenTest.php tests/Support/ChildStderrCaptureTest.php tests/Config/GlobFigureDriftTest.php </dev/null — expect exactly OK (176 tests, 31255 assertions); a different assertion count after your F5 edit means SymbolCitationDrift or a cousin counts comment text — reconcile per-class and report, do not paper over.
2. FULL suite from the sandbox CHECKOUT ROOT: cd /home/sites/prompt-step-P3.CLOSE-r3-fix && sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --log-junit /home/sites/prompt-scratch/P3.CLOSE-r3/fix-1/suite-fix1.xml </dev/null — write prediction.md in your scratchpad FIRST predicting 10556 tests / 163806 assertions / 1 skipped / 0 failures (or a documented reason for any expectation you change — you should have NONE, all edits are prose and comments). Then run. Reconcile with: python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py <your suite-fix1.xml> /home/sites/prompt-scratch/P3.CLOSE-r3/review-1/suite-r3.xml (branch figure first). ANY mover must be attributed to one of your six edits — if one cannot be attributed, REVERT your last commit, re-run, and report; do not merge on an unexplained mover.
3. Path-repo gate from the sandbox repo root: php tools/check-path-repos.php --no-lib-path-repos → exit 0.

## Final report (max 120 lines)
Sections: (1) the VERBATIM-brief md5 plus confirmation you wrote it; (2) per finding F1-F6: before/after quote (one line each) + file; (3) the grep-evidence output showing no unrepaired 320/2992x occurrences in live records; (4) fresh WorkflowEngine line numbers you re-derived; (5) census + suite figures with cwd, probe result, and cmp.py output summary; (6) golden md5s before/after; (7) commit list with sha + author check; (8) anything surprising.
