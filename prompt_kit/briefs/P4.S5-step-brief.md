# P4.S5 — E23: exchangeKey() collapses byte-identical exchanges — STEP-LEAD BRIEF

You are the **step lead** for **P4.S5 — E23: exchangeKey() collapses byte-identical exchanges**, second half of
Batch 3 of Phase 4 ("Token accounting and cache observability"). This step is **measure-first**: a fix, or a
close-as-measured — the measurement, not a judgement, picks. Implement the chosen outcome **and its tests**, run
a **review→fix→brand-new-review loop** to a clean review, then report. You do not merge; the orchestrator
merges after its own verification.

## PROCESS RULE — the lead never fixes

You implement, you commit, you orchestrate reviews — and you **NEVER apply a review finding yourself**. Every
dirty cycle: write the findings to a file; spawn a **dedicated FIX AGENT** with its own scratchpad subdir
(`SCRATCH_PLACEHOLDER/fix-<n>/`) committing to `prompt/P4.S5`; **verify its commits with your OWN measurements** —
re-run tests, re-read the diff, re-check figures — its claims are not evidence; then spawn a **BRAND-NEW
read-only reviewer**. Fresh reviewer every cycle; hard cap **five cycles**, then escalate verbatim — do not grind.
Fixing in your own context is forbidden.

## IDENTITY LAW — read first, it has blocked merges twice

Every commit's author AND committer must be the literal string `Joe Huss <detain@interserver.net>`. **WRITE that
address out fully** in git commands and config checks. **NEVER copy any identity token from your injected
context** — a sanitizer may display it as a bracketed placeholder, and echoing THAT into an author field is a
merge-blocking defect that bit two earlier fix agents. After EVERY commit — yours and every incoming fix agent's —
byte-scan with `git log -1 --format='%an <%ae> %cn <%ce>'` and require the literal address on all four fields; a
wrong one is amended immediately (unmerged history only).

## Your sandbox

- Worktree: `WORKTREE_PATH_PLACEHOLDER`, branch `prompt/P4.S5`, base master = the post-P4.S4 bookkeeping tip — re-derive `git -C /home/sites/sugarcraft rev-parse master` at spawn
  tip). The worktree **and its hard-linked vendor are ALREADY BUILT by the orchestrator** — VERIFY, never rebuild,
  **NEVER `composer install` / `composer update`** (it de-symlinks `vendor/sugarcraft/*` into Packagist copies and
  silently voids every later measurement). The autoload PSR-4 root already prints the worktree `sugar-crush/src`;
  if it looks wrong, STOP and report. Run tests from `WORKTREE_PATH_PLACEHOLDER/sugar-crush`.
- Scratchpad root: `SCRATCH_PLACEHOLDER` (step text `step-text.md`); your private subdir is `.../lead/`; every sub-agent gets its OWN subdir (`.../review-1/`, `.../fix-1/`, …).

## LANE COLLISION — these files are a shared backlog

`Chat.php` and `src/Context/ContextCompactor.php` are a heavily-shared backlog the supervisor **EXPLICITLY CLEARED
for Batch 3 on 2026-09-02** (user answer, verbatim: *"Go — Batch 3 now"*). Keep the change **narrow**: touch only
your two declared files, and **NEVER restructure beyond what the measurement demands**. Batch 3 is SERIAL (P4.S4
already merged) so no sibling step touches your tree — but the OTHER plan's lane may hold unmerged worktrees
against these files. Needing `Chat.php` = **STOP and report — it is NOT in your declared list.**

## Base figures

MASTER SUITE (orchestrator, post-P4.S4 merge; the belt is the authoritative CURRENT-TIP figure):
gate at branch tip 0ca4c088d = Tests 10638 / Assertions 164995 / Skipped 1 / 0 failures, EXIT 0, cwd
checkout root, serial, </dev/null, probe 0. Full-suite BELT RE-RUN on master after the merge printed
the IDENTICAL figure (/tmp/p4s4-belt.out) — 164995 is the MMG-201 arm; 164992 is the same tree's
MMG-198 arm, never headline-adjudicated, cmp.py per-class first. Nine-file census
(HAND_MAINTAINED_CENSUS_SET, file list at tests/TreeWideGuardRosterTest.php:407-417) at this tree:
OK 176 tests / 31460 assertions, cwd sugar-crush/, serial, </dev/null — re-measure at YOUR base if
the bookkeeping tip differs. Roster derivation 67/83/181/440/0 UNMOVED; roster file 17/1101. YOUR
BASE = post-S4 master — re-derive it with git rev-parse master at spawn and note it beside every
figure.

Frozen facts regardless of the fill:

- **Goldens byte-identical:** `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt` md5
  `32ea749d84938811ac9331419cae7380`, `golden-agent-prompt.txt` md5 `ef0326dd38535aaa2f1d715919bff26e` — **must stay
  byte-identical.** If one legitimately MUST move, follow **§16.2's golden-regeneration protocol** (old→new md5,
  why-correct statement, leak scan) and **FLAG it in the final report** — never silently regen.
- **Derived roster = 67 / 83 / 181 / 440 / 0** (`tests/TreeWideGuardRosterTest.php`: 17 tests / 1101 assertions).
  `tests/Context/ExchangeSummaryTest.php` **already exists on master** (confirmed at brief-draft time) — adding
  test METHODS moves no testFiles figure. 440 moves ONLY if you create a genuinely NEW test FILE (440→441),
  recorded honestly in the SAME commit — the P4.S2 brief once prescribed a false "NEW->441"; never record a
  figure you did not measure.
- **`ContextCompactorTest` now runs 80 tests / 287 assertions** (reported after the P4.S4 merge) — re-measure as
  your baseline before changing a line; its pins are load-bearing.

## Read these before you write a line — in `WORKTREE_PATH_PLACEHOLDER/`

1. **`SCRATCH_PLACEHOLDER/step-text.md`** — verbatim. Goal: *"Two byte-identical exchanges collapse to one key and
   one is lost. The backlog's own note: 'do not leave a judgement standing as if it were measured.' Measure it
   first, then fix or close."* Source §12.2 (E23); depends P4.S4.
2. **`src/Context/ContextCompactor.php`** — `exchangeKey()`'s definition and EVERY call site, then what the
   consumer actually does on a key hit vs a miss.
3. **`tests/Context/ExchangeSummaryTest.php`** — does any existing test drive two same-content exchanges?
   Measure, don't assume.
4. **`prompt_plan.md` §1.10** (removal is not an available outcome), **§1.11** (what counts as a test — **graded
   harder than the implementation**), **§17** (invariants), **§1.4** (the nineteen-check review brief; YOU hand it
   to each reviewer, verbatim), **§16.2**, **§16.8 rules** 2, 15, 40, 42, 44.
5. **`prompt_expand.md` §12.2** — the E23 row only, not end to end. Plus `CLAUDE.md` / `AGENTS.md`.

### MEASURE-FIRST pointers — orchestrator measurements at post-P4.S4 master; re-derive in your tree anyway

Re-derived by the orchestrator AFTER the P4.S4 merge landed. At tip 0ca4c088d (tree == master post-merge,
`grep -n exchangeKey src/Context/ContextCompactor.php` + read): definition at **:96-99** returns
`hash('sha256', $userMsg . chr(0) . $assistantMsg)` — not md5, not role-prefixed; call sites at **:625**
('key' => self::exchangeKey(...) in the compact loop) and **:1200** (the summary lookup, ... ?? null);
`serializeExchange` gets **zero grep hits**; the file is **1248 lines**.

Two earlier pointer sets are RETIRED: the P4.S4 cycle-7 reviewer's pre-research (md5 of role-plus-content at
~:1101-1107, sole call site :559, serializeExchange() at ~:1143-1147) matched NO tree; this brief's first
draft corrected it with PRE-S4 numbers (:406/:981, file ending :1029) that P4.S4 then shifted. LAW:
**cite by function name PLUS line number — line numbers rot under your feet.**

What survives every reading: the key hashes the two texts ONLY — toolCalls/toolResults are NOT in the hash —
so two exchanges with byte-identical user+assistant text but different tool payloads WOULD collide; and the
function's own docblock (:90-93) asserts, unmeasured, *"Two byte-identical exchanges collide onto one key,
which is harmless — they would receive the same summary either way."* **That sentence is the judgement this
step exists to audit.** Whether a collision is reachable in real histories is UNMEASURED. Measure first:
fix (a test reproduces the collapse and the fix stops it) OR close as measured with the exact measuring
command in your report.

## Your declared file list — exactly two files

```
sugar-crush/src/Context/ContextCompactor.php
sugar-crush/tests/Context/ExchangeSummaryTest.php
```

**Do not touch anything else.** A finding outside this list is **REPORTED, never edited** — the lists make the
batch safe and widening one is an orchestrator decision; if the step cannot complete inside these two files, stop
and report with the specific reason.

## What "done" means — TWO legitimate outcomes, graded by measurement

The step text's Done-when, quoted: *"either a test reproduces the collapse and a fix makes it stop, or the worklog
entry carries the measurement showing it cannot occur in practice and the finding is closed as measured, with the
command that measured it."*

- **Outcome (a) — FIX:** a test reproduces the collapse (RED on the unfixed code — revert experiment mandatory)
  and a production change makes it GREEN, with a known-answer assertion.
- **Outcome (b) — CLOSE-AS-MEASURED:** the worklog names the **exact command**, the tree, and the **raw observed
  output** showing the collapse cannot occur in practice (or only where the consumer makes it provably benign).
- The ledger must **name which outcome you reached and why the measurement forced it** — a hedge fails the step.

## HAZARDS — the heart of this step, read five times

1. **"Fixing" an unreachable collapse** with production code when the honest outcome is close-as-measured — or the
   inverse. The plan warns: *"do not leave a judgement standing as if it were measured."* A fix for a defect never
   demonstrated reachable is a second unmeasured judgement wearing a green suite.
2. **The collapse may be REAL but BENIGN.** Grade what the consumer DOES on a key hit: losing a duplicate summary
   (both exchanges receiving the same one-line summary) vs losing USER CONTENT are very different.
3. **Scope walls.** Do NOT widen into `countTokens` tool_calls-blindness or the draft-echo over-window issue —
   recorded travel-ledger items for other steps; out of scope here (mention only in the final-report surprises
   section if the measurement naturally illuminates them).
4. **P4.S4 JUST merged truncation machinery into this same file.** `ContextCompactorTest` now runs 80 tests / 287
   assertions and its pins are load-bearing — keep every one green; do not weaken P4.S4's bounded-rise test.
5. **Same-file edit etiquette.** Batch 3 is serial — no concurrent sibling step — but the other plan's lane may
   have UNMERGED worktrees against `Chat.php`. Needing `Chat.php` = STOP and report.

## THE TWO THINGS MOST LIKELY TO MAKE THIS STEP WRONG

1. **A judgement dressed as a measurement.** Close-as-measured with no command in it, or a fix whose reproduction
   never went RED, is the defect this step exists to erase: (a) without a revert-red, or (b) without a pasted
   command + raw output, is not that outcome — it is unmeasured.
2. **Grading the harm without reading the consumer.** "Keys collide" proves nothing until you show what a
   collision DOES at the consumption site. Drive the real path twice with same-content exchanges and assert the
   observable consequence — that IS the measurement in both directions.

## Step-text falsification clause (§16.8 rule 44)

If the step text's shape proves false on the live tree — e.g. the key is sha256 of content only rather than md5 of
`role."\n".content`, or no production call site can ever see two same-content exchanges — **report the measured
truth in three-part form: what it SAID / what is TRUE / how you MEASURED it.** Never silently edit the plan or
"adjust" this brief; a claim measured false is the most valuable finding.

## §1.11 — WHAT COUNTS AS A TEST HERE. READ THIS TWICE.

These do **not** count: annotations (`@covers`, `@test`); existence checks (`method_exists()`, `class_exists()`,
`is_callable()`, `property_exists()`); shape assertions (`assertNotNull`, `assertIsArray`, `assertTrue(count(...)>0)`).
A real test calls the thing, asserts the **value** and **exact counts**, covers **both polarities** +
**pathological input**, goes red when reverted.

**Experiments this step MUST run and report.** Outcome (a): the reproduction test **goes RED on the unfixed code**
— exact revert edit, command, red test name and count, restoration confirmed — and the fix carries a
**known-answer assertion** (expected behaviour as a value, not "didn't throw"); every new guard gets its own
deletion experiment. Outcome (b): the worklog names the **exact command, the tree, and the observed output** —
someone else must run it and see the same thing. **Unrun experiments fail the step however green the suite.**

## VERIFY BEFORE YOU FINISH

From `WORKTREE_PATH_PLACEHOLDER/sugar-crush` unless stated otherwise:

1. **Your own test files, by path** (not `--filter`): `vendor/bin/phpunit tests/Context/ExchangeSummaryTest.php`
   then `vendor/bin/phpunit tests/Context/ContextCompactorTest.php`
2. **The nine-file tree-wide census set — ALWAYS** (these walk `src/` and `tests/` wholesale; the list is KNOWN
   INCOMPLETE; check the ASSERTION COUNT — a roster test that stops iterating still prints `OK`):
   ```sh
   vendor/bin/phpunit \
     tests/SymbolCitationDriftTest.php \
     tests/SwallowingCatchCensusTest.php \
     tests/Support/DuplicatedTestHelperDriftTest.php \
     tests/Support/ChildWallClockBudgetTest.php \
     tests/Config/EnvRosterDriftTest.php \
     tests/Tools/BuiltInToolCorpusTest.php \
     tests/Support/InterpolationOpenerTokenTest.php \
     tests/Support/ChildStderrCaptureTest.php \
     tests/Config/GlobFigureDriftTest.php
   ```
   Expected figures: the Base figures block (a census figure names its file list AND its tree). Also
   `tests/TreeWideGuardRosterTest.php`: derived **67 / 83 / 181 / 440 / 0**, expected UNMOVED (your new tests go
   INTO the existing ExchangeSummaryTest.php); any moved figure is updated in the SAME commit, old→new.
3. **Golden md5 check** — `md5sum` both pinned files against Base figures, from the worktree root.
4. **Path-repo gate, RUN FROM REPO ROOT** — `WORKTREE_PATH_PLACEHOLDER`, not `.../sugar-crush` (the wrong-cwd
   misread has happened twice — the "failure" is fake):
   `cd WORKTREE_PATH_PLACEHOLDER && php tools/check-path-repos.php --no-lib-path-repos`. Never add a
   `repositories[]` entry to `sugar-crush/composer.json`; never commit a `sugar-crush/composer.lock`.
5. **Full `sugar-crush` suite — ONLY at the end, from the CHECKOUT ROOT, serial, stdin redirected, box quiet:**
   ```sh
   ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'     # must print 0 BEFORE any suite run
   cd WORKTREE_PATH_PLACEHOLDER && php sugar-crush/vendor/bin/phpunit \
     -c sugar-crush/phpunit.xml --colors=never </dev/null | tail -5
   ```
   - **If the probe is nonzero and that phpunit is NOT yours, do NOT run your own full suite — report instead**
     (an orchestrator gate may run concurrently early on; concurrent suites poison every figure).
   - **Write `SCRATCH_PLACEHOLDER/lead/prediction.md` BEFORE the run** — predicted figures and why, against the
     Base figures baseline. Name the cwd beside every figure; always `</dev/null` —
     `Chat\CompactModelSummaryTest` and `MouseModalGuardTest` fail ONLY under a live tty. The ±3 MouseModalGuard
     arm (198 vs 201) is a documented viewport artifact — **NEVER headline-adjudicate it**; reconcile per-class:
     `--log-junit` both sides, then `python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py <branch.xml>
     <base.xml>` — **branch xml FIRST, base xml SECOND**; copy `cmp.py` into your own scratchpad subdir first.
     Use **`/usr/bin/grep`** for whole-tree scans (shell `grep` = `ugrep`). Totals are deterministic across
     sequential uncontended runs — never dismiss a delta as noise.

php-cs-fixer is NOT installed on this box; the style gate cannot run locally — match the code by eye.

## THE REVIEW LOOP YOU RUN — this is most of the work

1. Implement the measured outcome, write the tests, **and COMMIT to `prompt/P4.S5` immediately.**
2. Spawn a **read-only review agent**: verbatim §1.4's nineteen checks, your step text, the diff at the
   **current** tree position, your own test output; own subdir `.../review-<n>/`.
3. **Write its findings to `.../review-<n>/findings.md` the moment you receive them** — **EIGHT of one step's ten
   findings were LOST to a context boundary**.
4. Dirty → **spawn the dedicated FIX AGENT — you never fix (PROCESS RULE)** → it commits → **you verify its
   commits with your own measurements** → spawn a **brand-new reviewer**. Loop.
5. **Break only on a clean review.** Cap **five** cycles; then stop and report blocked, verbatim.

**A new reviewer is NEVER handed the previous reviewer's findings, and is never told a previous review happened**
— a reviewer given a list checks the list. **Never reuse a reviewer.** Tell each reviewer both outcomes (a) and
(b) are legitimate — grade whether the measurement performed establishes the one claimed.

**If any agent comes back empty, aborted, or truncated: it DIED** — a blank reviewer is **not** `NO FINDINGS`.
Resume once, else relaunch in the same worktree told not to start over; **five** attempts; **never write a dead
agent's report yourself.** **Never leave a sub-agent's work uncommitted** — commit immediately.

## HARD PROHIBITIONS

- **Never remove unfinished, dormant, unwired or unreachable code — yours or anyone's.** The three permitted
  outcomes are **wire it**, **build it out**, or **stop and ask the orchestrator**, including quiet forms
  (stubbing a body, dropping the last call site, `@deprecated`-ing it aside, deleting a test that pinned a
  dormancy). Full rule: §1.10. **Escalating is a completed outcome.**
- **Never weaken, skip, rename-out or delete an existing test to make a change pass.** Every test in the two
  declared test files today must still exist and still run; if one becomes wrong because behaviour genuinely
  changed, say so explicitly with reasoning.
- **Stay in the two declared files; stay in E23.** No reaching into `Chat.php`, no `countTokens` widening, no
  draft-echo work, no opportunistic ContextCompactor.php refactoring — report instead. **Never close on
  argument:** if your reasoning says the collapse cannot occur, the worklog must contain the SHOWING command.
- **ORCHESTRATION-RULE-2 — no scratch git repo outside your own scratchpad subdir; verify `pwd` before ANY
  `git init` / `git commit` / `git config`.** A previous reviewer ran throwaway-repo setup inside
  `/home/sites/sugarcraft` itself and left a stray commit on **master**. **Never `git config --global`; never
  `git config` where you did not create the repo.**
- **ORCHESTRATION-RULE-3 — every agent gets its OWN scratchpad subdirectory and may `rm -rf` only inside it.** No
  generic names at the scratchpad ROOT (`sb`, `base`, `count.php`, `*.orig.php`); back any mutated worktree file
  to a PRIVATE path — a shared restore name can silently restore ANOTHER agent's version.
- Never `git push`; never merge into `master`; never run `caliber`; never `composer install` / `composer update`;
  never touch `/home/sites/sugarcraft` or `/home/sites/crush-lane-{a,b,c}`; never modify
  `docs/plans/crush_code_*.md` or `left_steps.md`; never suppress a hook (`--no-verify`, `core.hooksPath`); never
  global-`pkill`.
- Never write a number you did not measure. Every figure names its command and cwd.

## YOUR FINAL REPORT

1. What you changed, file by file, with line ranges — for outcome (b): what you did NOT change.
2. **The Done-when ledger:** which of the two outcomes you reached and why the measurement forced it — each clause
   with its evidence, a file:line or a test name. **For (b) paste the exact command + raw output; for (a) paste
   the RED-on-revert proof.** A clause with no evidence is an admission the step is not done.
3. **The falsification / deletion experiments:** (a) exact revert edit, command, red test name and count, restoration
   confirmed; (b) the reproducible measurement command with tree and output.
4. **What the collision does at the consumer** — every call site graded with observable evidence (hazard 2).
5. **Your figures**, each naming cwd and command: your test files by path; the nine-file census set (file list +
   tree named) plus `TreeWideGuardRosterTest` 67/83/181/440/0 with any moved figure stated old→new; the full suite
   with PREDICTION stated first and the ±3 MouseModalGuard reconciliation; the golden md5 check; the path-repo
   gate from repo root.
6. **The review loop:** cycles, every finding verbatim with what the FIX AGENT did, pointers to the findings
   files, and how you independently verified each fix commit — plus the identity check (`%an <%ae> %cn <%ce>`) for
   every commit on the branch.
7. **Everything found outside your two declared files**, with `file:line` — reported, never edited.
8. **Anything measured that contradicts the step text or this brief** (rule 44 / rule 42 three-part form: said /
   true / how measured), **surprises**, and any follow-up for its own step.
