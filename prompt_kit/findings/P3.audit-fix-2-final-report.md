# P3.audit-fix-2 — the continuation agent's final report

**Recorded verbatim by the orchestrator, 2026-09-01.** This is the report of the §1.8 rung-3
recovery agent, reproduced as received. It is kept because the FIRST fix agent on this step died
without ever producing one, and that absence cost a full recovery cycle; a report that lives only
in a harness transcript is one `/clear` away from being the same absence again.

**Provenance and status.** Branch `prompt/P3.audit-fix-2`, HEAD `067a18e0a`, synced to master at
`d3eaa97a8`, merged to master as `980670c0b`. Twenty-five commits: eleven from the agent that died,
fourteen from this one. **The figures below are the AGENT's. Every one of them was independently
re-measured by the orchestrator before the merge and all reproduced exactly** — see the merge
commit `980670c0b` and the worklog entry for the orchestrator's own numbers, which are the ones
that decided the merge.

---

## 1. The A1–A7 ledger

| | finding | landed at | proved by |
|---|---|---|---|
| **A1** | F7 — the false "the two order `<env>` oppositely" reason, live in two production doc-blocks | `src/Runtime.php:771-806`, `src/Agents/Agent.php:475-511` (rule-42 three-part form); pins at `tests/RuntimeTest.php:1808` (code) and `:2130-2310` (prose, two channels over a derived `src/` domain) | trailer appended after each assembler's env render → red; live claim planted in a third `src/` file, as a comment and as a string constant → red each; correct re-wrap → green |
| **A2** | F4 — the assertion message repeating the falsified "unwireable" claim | message rewritten `tests/Agents/AgentTest.php:2278-2291`; made executable by `testTheFalsifiedPerStageWriteSignalClaimSurvivesOnlyInsideAQuotationOfWhatThisMessageUsedToSay()` | revert the message → red, "Do not restore it"; claim planted elsewhere → red naming `file:line`; correct quotation → green |
| **A3** | F6 — the self-census was 30/46 and wrong the day it landed | `src/Agents/Agent.php:419-455`, **31 distinct / 54 occurrences**, both pipelines printed; pin at `tests/Agents/AgentTest.php:1631` | bump either literal → red; add a citation inside the block → red; add one outside → red on the domain assertion |
| **A4** | F2 — the write-primitive scanner blind to `T_NAME_RELATIVE` | alphabet at `tests/RuntimeTest.php:3657`, rewrite arm `:3680`, limits enumeration updated, test at `:4824` | drop the token class → **exactly one** failure, that method |
| **A5** | F1 — the nine-file census set is hand-maintained over a derivable population | `tests/TreeWideGuardRosterTest.php` (new, 16 tests / 1082 assertions), roster **derived at 67** from 440 test files, `unaccounted 0` | blind channel A → 5 red; drop a row from any of the four hand-maintained constants → red; plant an unresolvable walk → red naming file+expression; plant a correct new guard → green, absorbed |
| **A6** | F3 — the `</env>` fence escape via the git branch name | `tests/Context/EnvironmentBlockTest.php:1056-1275` — recorded as an executable pin, **not fixed** | escape `<`/`>` in the branch read → red, "must reach the block unescaped — see this test's docblock before 'fixing' it"; cap it → red |
| **A7** | F9 residual — the message named the wrong side of a real divergence | `tests/RuntimeTest.php:2942-2949`, both messages | gate reads the authority → 1 test / 6 assertions / 1 failure naming PermissionGate; Runtime's arm broken → 1 test / 7 assertions / 1 failure naming Runtime; full-suite prefix respell → **17** failures |

All seven landed. Six are closed cleanly; A5's *instrument* was the thing every review cycle kept
re-opening.

## 2. What the continuation agent changed, file by file

- **`tests/TreeWideGuardRosterTest.php`** — capped taint loop → true fixpoint; `GlobIterator` added
  to the alphabet; a removal half for the declared-guard rows; `assertCount` liveness pins on all
  four hand-maintained constants and both alphabet halves; zero-live-site spellings must carry a
  known-answer fixture, and that correspondence went text → token-stream → **execution-derived**
  (`knownAnswerSources()` run through the shipped classifier); the fixture key set pinned; ~15
  present-tense population cardinalities removed or given an owner; three mis-cited rules fixed.
- **`tests/RuntimeTest.php`** — A1 prose pin rewritten four times: line-scoped → flattened →
  token-scoped per comment + a second code channel → licence keyed on the rule-42 **three-part
  form**; the correction-survival pin went count → `file:line` list → **file + quotation** list; A4
  alphabet widened; A7's bare `assertTrue` given a message; stale figures dropped in favour of
  generators.
- **`tests/Agents/AgentTest.php`** — A2 message rewritten; A2 census built (`src/` + `tests/`, three
  buckets: licensed / drifted / live), licence now requires being **inside the still-open
  quotation**; offenders reported as `file:line` by ordinal; the A3 domain assertion names a line.
- **`tests/Context/EnvironmentBlockTest.php`** — untouched by the continuation agent; the
  predecessor's A6 pin verified and mutation-proven.
- **`src/Runtime.php`**, **`src/Agents/Agent.php`** — doc-block only. Executable tokens byte-identical
  to master (4366 and 1270 tokens, md5 identical), verified independently by three reviewers **and
  again by the orchestrator** before merge.

## 3. Deletion experiments

Roughly **130** mutations across the eight review cycles it ran plus its own. Every guard the branch
ships was broken and watched go red; every guard was also given a *correct* change to tolerate. Two
mutations survived and are reported honestly in the tree: removing the `closeOverDelegates()` call
site changes nothing on this tree (the file declares this), and dropping only the token-class filter
in `namesOneOf()` while still comparing exact token text.

**The one recurring finding, across nine consecutive cycles, was always the same shape: a check
satisfiable by something other than executed code.** In order: a comment; an assertion message; an
array literal nothing compares; a tautological `assertSame`; a string literal spelling the assertion
out; the message again once the gate moved to the token stream; a dead ternary arm; a bare
`str_contains` letting a spelling ride on its longer sibling; and finally an unasserted fixture.
Each fix narrowed *which text* counted, and the next reviewer walked through the next door. The last
two commits stopped grading text at all — coverage is now what the shipped classifier emits — and
pinned the fixture set so a tenth fixture cannot grant coverage silently.

## 4. The agent's figures

```
tests/RuntimeTest.php                      OK (130 tests,  488 assertions)
tests/Agents/AgentTest.php                 OK ( 35 tests,  402 assertions)
tests/Context/EnvironmentBlockTest.php     OK ( 43 tests,  170 assertions)
tests/TreeWideGuardRosterTest.php          OK ( 16 tests, 1082 assertions)
nine-file census set                       OK (176 tests, 31245 assertions)   master 31215
derivation   roster 67, candidates 83, walkerFiles 181, testFiles 440, unaccounted 0
FULL SUITE, cwd = checkout root            Tests: 10547, Assertions: 163710, Skipped: 1  (07:03)
master baseline, same worktree             Tests: 10526, Assertions: 162447, Skipped: 1
check-path-repos --no-lib-path-repos       exit 0
```

**Figures it could not reproduce: none outstanding.** Roughly a dozen were found stale during the
cycles — `22`, `1071`, `SEVEN` markers, `6/8` odd-parity, `five` residue occurrences, `65→67`,
`472`, `32` — and every one was either re-derived and corrected in rule-42 form or replaced by its
generator.

## 5. Findings outside the six declared files — reported, never fixed

1. `prompt_expand.md:309` and `:3755-3758` still carry the A1 falsehood at its **source document**,
   `:3755` reproducing the §17.2 paragraph verbatim including the two citations F7 measured as
   rotted. **This is the propagation source and it is still open.**
   → **CLOSED by the orchestrator in `cf63f5007`, at FIVE sites rather than the two reported.**
2. `prompt_plan.md` §1.2 action 7b still lists **nine** files where the derivation answers **67**.
   Until the orchestrator updates it, A5's derivation is built and never consulted.
   → **CLOSED by the orchestrator in `cf63f5007`.**
3. `src/Context/EnvironmentBlock.php:288` argues the branch read needs no cap because a ref is
   bounded by the 255-byte filename limit. **That is per path component; a 359-byte multi-segment
   ref reaches the block in full.** — OPEN, folds into P5.S3 with A6.
4. `src/Permissions/PermissionGate.php:691` hard-codes `'mcp__'` while `Runtime` reads the
   authority; a legitimate respell moves them apart **in the permissive direction**. — OPEN.
5. `tests/Support/ChildStderrCaptureTest.php:199-204` keys `'Context/'` by prefix **with no count**,
   so this branch's ~14 new `shell_exec('git … 2>/dev/null')` sites are absorbed silently. — OPEN.
6. `sugar-crush/phpunit.xml`'s doc-comment pins "all 6465 tests"; the tree runs 10547. — OPEN.

## 6. Contradicting the brief

- **What the brief said:** the previous agent died mid-flight and its commit subjects are a lead,
  not evidence. **What is true:** its work was real and largely sound — **but of the seven figures
  it wrote into the tree, six did not reproduce.** **How measured:** re-derived each with its own
  stated generator at its own stated commit.
- **What the brief said:** "if you change anything, run one more review cycle." **What is true:**
  that terminates only if a cycle finds nothing. **Eight cycles ran; the first seven each found
  real, mutation-provable defects.** **How measured:** each cycle was a brand-new agent handed
  §1.4's nineteen checks verbatim, the diff and the test output — never the previous reviewer's
  findings.

## 7. What the predecessor left undone or wrong

A5 landed as a 2,600-line derivation with a capped 8-pass taint loop, no removal half on its
declared rows, `GlobIterator` a silent fail-open, ~12 present-tense population cardinalities in a
file whose thesis is that such figures rot, and six wrong figures. A1's prose pin did not exist at
all — the correction was doc-block text nothing enforced. A2 had no executable pin. All closed.

## 8. Where the agent left it

Cycle 12's verdict was **"this branch closes A1–A7"**, with two findings — both inside the declared
file list, both fixed and mutation-proven in `067a18e0a`. The agent's own judgement: cycle 12 was
the first to return a "closes A1–A7" verdict and its two findings were the narrowest yet — one
required deliberately adding an unasserted fixture, the other was a maintenance liability rather
than a hole. It offered to run cycle 13 or to call the step done.

**Orchestrator's disposition: merged without cycle 13**, because the next action in the queue is the
Phase 3 CLOSE REVIEW CYCLE 2 — a brand-new reviewer over ALL of Phase 3's commits as one
change-set, which now includes these twenty-five. That reviewer subsumes cycle 13 and sees the
merged state rather than a branch. Reversible: anything it finds becomes an ordinary fix step.
