# Chart V5 Plan — geometry activation + width wiring round (chart_v5_plan.md)

> v5 round. Companion: chart_backlog_plan.md (v3, COMPLETE), chart_v4_plan.md (v4, COMPLETE), chart_plan.md (v2), chart_worklog.md (ledger), chart_prompt.md (protocol v3). HEAD at intake: d549c0d48 (tree CLEAN). **RULINGS RECEIVED 2026-09-03T18:25Z — build spawns UNLOCKED; see ## Rulings R1..R5 (authoritative for D1..D6).** Every implementer brief goes BY PATH: "read chart_v5_plan.md step D<n> + Ground rules". v3 machinery UNCHANGED: direct master, NEVER push, one commit per step, write-sets are ceilings, test-count arithmetic declared. Ground rules carry over from chart_plan.md §§1-12 (style, flags-default-off, two golden systems, GR-12 with B12 as the sanctioned sweep exception, GR-11 reverse-dep runs).
>
> **Floors @intake**: charts@c8b40f667: 570 tests / 1404 assertions / 0Sk rc0 under failOnWarning. dash@0831738ad: 5924 / 9621 / 1Sk / 0W rc0 under failOnWarning (sole skip = DashboardLiveTest cassette, untouchable). core@15a9900ed: 849 / 7586 / 27Sk / 0W (measure via redirect, NEVER pipe — a core test stdout-fseeks; `| tee` corrupts counts). Golden work = TARGETED reflection replay ONLY (/tmp/b10a-replay.php recipe; blanket tools/generate-goldens.php FORBIDDEN); AliasCompletenessTest census pin 125 is CHARTS-only — no v5 step adds charts public withers/aliases (D2/D3 add GaugeCircle methods in dash; dash has no method-list census — verified live).
>
> Recon sources (all @d549c0d48, dated 2026-09-03; load-bearing cites RE-VERIFIED live at intake): /tmp/v5-reconD.txt (GaugeCircle: setSize stores-never-reads + live probe, goldens inertness fingerprint, Donut port inventory ≈25-30 sites, consumer/break analysis), /tmp/v5-reconE.txt (bufferFromOutput row-clip scratch PROOF + exact test-move list, Bubble identifier census + rename sets, **Width.php discovery**, plot-braille measured truth). Premise verdicts: row-clip TRUE · GaugeCircle setSize TRUE · Bubble rename TRUE · **Width: CHANGED-with-evidence** — the v4 "no wcwidth machinery repo-wide" premise is FALSE: candy-core/src/Util/Width.php (final class, 1028 LOC, isWide() UAX table :964+, ANSI-aware, memoized) is a live production primitive, both sugar-dash and sugar-charts already require sugarcraft/candy-core, dash imports Core\Util\Width 112× (verified count @d549c0d48; live probe Width::string('日本')=4) → D4 becomes a WIRING task · plot-braille TRUE (panels byte-identical; "MarkerDot" exists nowhere as code; echo :37 IS in output+goldens).

## Rulings

> RULINGS RECEIVED 2026-09-03T18:25Z — build spawns UNLOCKED; authoritative for D1..D6. Evidence refs retained (reconD/reconE, both @d549c0d48).

**R1 — D1 row-clip: APPROVED variant (a).** Callers :190/:198 pass the FULL emitted frame height to bufferFromOutput (width-clip stays status-quo-PARKED); the TWO mirror blocks (:795-799, :922-933) update to the new geometry + ONE stale-label-nonzero-delta regression test lands (label-only mutation after first render ⇒ render() delta non-empty). Binds D1.

**R2 — D3 aspect default: MATCH DONUT — withAspect default-ON, 2.0 semantics (null⇒2.0)** for GaugeCircle. Consequences (written into D3 + D2): (i) D3's default-path clause FLIPS from "default byte-identical to post-D2" to DECLARED-OUTPUT-CHANGE — D3 RE-RECORDS gaugeCircle goldens ×2 (the SECOND re-record after D2's; targeted reflection replay only; D3 bodies take the post-D2 state as PRE-image; cross-check replay==targeted-suite PASS). (ii) D3 keeps the withAspect(1.0) legacy-escape test — geometry at 1.0 == D2-shipped bytes (this pin REPLACES the default-freeze role) — B5-guard message-parity tests stand. (iii) D2 keeps its OWN goldens re-record (activation) with the interim note that D3 supersedes these bodies with aspect-2.0 renders. D2's radius-when-setSize formula stays builder-designed and MUST be aspect-NEUTRAL (raw-cell geometry at D2; aspect arrives only at D3). Binds D3 + D2 consequence.

**R3 — D5 rename: SET 3 FULL SWEEP.** CIRCLE_CHARS→ROUNDED_BOX_GLYPHS, getCircleChar→glyphAtOffset, drawBubbleOnGrid→drawShapeCluster. ALL 15 src refs + by-name pins BubbleTest :459 (constant-name string) / :467 (fail-msg) + prose sites update; class-header vocabulary sweep :14-27 if names appear; history docs NEVER touched. PURE-rename byte-zero-movement gate STANDS (+ dash suite identical 5924/9621/1Sk/0W + render sha1 pin pre/post). Binds D5.

**R4 — D6 plot-braille: VARIANT (c) REAL COMPARISON.** Left panel `withMode(Plot::MODE_SCATTER)`, right stays default line; echo :37 labels → truthful pair (e.g. 'Scatter (MODE_SCATTER)' / 'Line (default)'); docblock + section comments honest; goldens plot-braille ×2 RE-RECORD via targeted replay (bodies genuinely differ panel-side now, not just dims). Builder MUST live-verify scatter renders recognizably — STOP + report if MODE_SCATTER broken/degenerate (recon cites setPoint dots at Plot.php:183-187). C6′ assign lines UNTOUCHED. Ceiling: examples/plot-braille.php + plot-braille.golden ×2. Declared-output-change: YES (labels + left panel shape). Binds D6.

**R5 — blanket: APPROVED.** (i) bug-class changes pre-approved with ASCII-pins discipline; (ii) D2 dead-import purge authorized (re-verify unused live at spawn); (iii) D3's 1-line Donut.php:99 provenance fix authorized (explicit ceiling addition to D3); (iv) D1..D6 in-round; Buffer.php:120 TODO + MARKER_BRAILLE vestige → v6 (Parked section already states). Binds D1-D6 + Parked.

## Steps

Baselines for ALL steps: charts 570/1404/0Sk rc0; dash 5924/9621/1Sk/0W rc0 (both failOnWarning live — rc0 ⇒ zero-W by construction). Every step re-derives its own quotes live at spawn (recon mtimes 2026-09-03).

### D1 (Lane B) sugar-dash: bufferFromOutput ROW-CLIP — axis+labels rows enter the diff buffer [R1]

**Why**: reconE SCRATCH PROOF (rc0, live): callers Chart.php :190/:198 pass getChartHeight() (= height ?? heightConstraint) as diff height, but renderBarChart/renderLineChart EMIT chartHeight (area) + showGrid?1 (x-axis) + showLabels?1 (label) rows — bufferFromOutput (:795, loop `$row < $height`) silently DROPS the axis+label rows from the stored diff buffer. Consequence PROVEN: plain CJK Bar (emitted 11 rows, stored 40x10) with a label-only mutation (日本→東京, values equal) renders delta = **0 BYTES** — stale labels persist forever on a delta-tracking terminal. Colored Chart::new: emitted 12 rows/50-rune longest → axis row 10 + label row 11 dropped AND cols 40..49 width-clipped (width clip = status-quo, stays parked). Existing tests dodge the hole by design: ChartTest :758 mirror :795-799 ("labels row sits outside the diff geometry") + direct-rebuild workaround :801-810 (reflection invoke at 40x11); :883 mirror :922-933 ("clip redesign parked", slice(0,40)+40x10).

**Design**: callers :190/:198 compute the diff height from the FULL emitted frame — `count(explode("\n", $output))` (raw lines == SGR-stripped lines: the strip is newline-free, builder re-verifies) — passed as $height to bufferFromOutput; resize detection stays keyed on chartWidth/chartHeight (ChartRenderContext prevWidth/prevHeight semantics), so the buffer grows 1-2 rows above the chart-area param — **R1 variant (a) APPROVED**: builder re-derives the exact minimal caller-side form (count vs param-widen within the approved variant; bufferFromOutput self-derivation variant (b) stays REJECTED — diverges Buffer dims from param). Width clip UNTOUCHED-parked (ChartTest :994-1000 mirror stays verbatim; :1004-1013 full-height pin already expects it). Update the TWO mirror blocks (:795-799, :922-933) to the new geometry; the :801-810 reflection workaround folds INTO the parity assert (becomes redundant). Explicit-dimension direct-call tests :835/:856/:943/:986 survive UNMOVED (verified move-list = exactly 2 tests). NEW regression test = the reconE proof as a repo test: label-only mutation after first render ⇒ render() delta NON-zero (contains the new label runes).

**Write-set ceiling**: sugar-dash/src/Plot/Chart/Chart.php [SYMBOLS: bufferFromOutput docblock :783-794 + call sites :190/:198 ONLY], sugar-dash/tests/Plot/Chart/ChartTest.php [SYMBOLS: :758 test mirror+workaround, :883 test mirror, ONE new test] ONLY.

**Verify**: full dash rc0; delta +1..2 tests / assertions +2..+8 (measure exact); goldens porcelain EMPTY (diff-internal); **full-frame stdout byte-frozen**: render() first-frame bytes + ALL goldens sha256 PRE==POST (C4 sha-gate precedent: chart.golden ×2 + dashboard-charts.golden ×2 minimum, enumerate live); temp-revert proof (clip-restored ⇒ exactly the new regression test dies). Absence: `/usr/bin/grep -n 'labels row sits outside\|redesign parked' sugar-dash/tests/Plot/Chart/ChartTest.php` → 0 hits post-step (both phrases replaced; :994-995 "status quo" width-clip comment SURVIVES).

**Declared-output-change flag**: internal-diff-state only (re-render delta stream corrects — bug class; stdout frame + goldens byte-frozen).

### D2 (Lane A) sugar-dash: GaugeCircle setSize geometry activation [v4 Q3 (f) revive]

**Why**: reconD PROVEN inertness — setSize (:65-71) clone-and-stores into fields :23-24, and the class has ZERO read-sites for $this->width/$this->sizerHeight (grep-verified @d549c0d48) ⇒ render() (geometry from ctor radius: diameter 2r+1) and getInnerSize (:202-209, [$diameter, $diameter+labelRow]) never consult the allocation. Live probe: `GaugeCircle::new(0.8)` vs `->setSize(40,10)` renders sha1-IDENTICAL (2186ff670a87f0500b70148b0356b504108f1d7a, strlen 1063 both); getInnerSize [13,14] both; reflection shows 40/10 stored-on-clone-but-unconsumed. Goldens gaugeCircle ×2 (80x24 1171B / 120x40 1173B) bodies BYTE-IDENTICAL sha1 645f785c6d54b5843e293c938b91c9c4dc2ba735 (re-derived live at intake via awk) = the inertness fingerprint in fixture form; harness replay LIVE-confirmed (replay@both dims == golden body; setSize(60,15)/(80,24)/(120,40) all → 645f…). examples/gaugeCircle.php:12 discards the clone. Second defect: all 9 withers (:216-355) use plain `return new self(...)` — allocation EVAPORATES through any setSize→wither→render chain; the production path exists: StackedGrid.php:201-202 `setSize(...)->render()`. ⚠ Also-true finding: Donut.php:99 provenance docblock ("Copied verbatim from GaugeCircle::ARC_CHARS") is STALE — B12 deleted that source const (GaugeCircle holds only NEEDLE '❮' :29 / CENTER '◆' :30) — fix rides D3 (1-line ceiling addition there).

**Design**: allocation-aware effective radius — direction (builder designs the exact formula; **R2 constraint: MUST be aspect-NEUTRAL — raw-cell geometry at D2, aspect arrives only at D3**): `effectiveRadius = max(3, intdiv(min($this->width ?? (2*$this->radius + 1), $this->sizerHeight ?? ($this->radius*2 + ($this->showLabel ? 1 : 0))) - ($this->showLabel ? 1 : 0), 2))`; **CONTRACT: setSize never called ⇒ render() byte-IDENTICAL to HEAD** (radius-only default path frozen — probe test + sha gate). All radius reads in render (arc :100-101, ticks :127/:136, needle :149, grid extent :80-85, bounds guards) + getInnerSize consume effectiveRadius. Withers adopt the GaugeWithDetail::withValue allocation-preserving pattern (same dir, precedent :192-205: new-self THEN `if ($this->width !== null && $this->sizerHeight !== null) return $clone->setSize($this->width, $this->sizerHeight);`) — all 9. NO ctor param insertion: if any param is unavoidable it MUST APPEND after labelColor — 9 positional `new GaugeCircle(...)` test call-sites (:99/:108/:381/:385/:388/:396/:397/:403/:404, incl. 6th-pos Color :397) make any earlier insertion a TypeError cascade. Goldens ×2 MUST re-record (example setSize becomes REAL ⇒ patchExample :145-166 injects canvas dims ⇒ 80x24 vs 120x40 bodies DIFFER — declare + show content; targeted reflection replay only, double-replay sha determinism proof). **Interim per R2: D3 will supersede these bodies with aspect-2.0 renders — the D2 re-record still stands as the activation proof (and D3's PRE-image).** Optional **[R5-ii authorized]**: examples/gaugeCircle.php :6-8 dead-use purge (ChartDataPoint/Options/ItemOptions — verified unused live at intake; RE-VERIFY unused at spawn) + assign :12 (`$component = $component->setSize(60, 15);`, C6′ precedent pattern) so the example exercises the mechanism.

**Write-set ceiling**: sugar-dash/src/Plot/Chart/GaugeCircle.php [SYMBOLS: render preamble + radius read-sites, getInnerSize :202-209, withers :216-355, setSize :65-71, ctor/::new() ONLY IF unavoidable], sugar-dash/tests/Plot/Chart/GaugeCircleTest.php, sugar-dash/tests/golden/{80x24,120x40}/gaugeCircle.golden, sugar-dash/examples/gaugeCircle.php [SYMBOLS: :6-8, :12 — optional].

**Verify**: full dash rc0, floor 5924/9621/1Sk/0W; delta +2..4 tests (allocation survival through full wither chain; default-path byte-identity vs HEAD-state probe sha 2186ff67…; setSize(40,10) actually shrinks — geometry observable; getInnerSize allocation parity); goldens: gaugeCircle ×2 body-churn DECLARED (old 645f785c → new, double-replay sha1-identical, bodies cross-dim DIFFERENT); zero other-golden movement; existing C8a predicate pins :378-406 + radius-ordering tests stay GREEN unedited (size-insensitive at fixed radius 6 — reconD §5 analysis); census 125 UNTOUCHED.

**Declared-output-change flag**: YES — setSize path activates (gaugeCircle.golden ×2 re-record); never-setSize default path byte-frozen by test.

### D3 (Lane A, serial after D2 — same files) sugar-dash: GaugeCircle aspect + quadrant-rim port from Donut [v4 Q3 (g) revive] [R2]

**Why**: gauge geometry is raw-cell-unit (visual 2:1 vertical ellipse in terminals). Donut reference surface VERIFIED live: DEFAULT_ASPECT=2.0 :34, RIM_SAMPLES :44-49 (quadrant offsets+bits), QUADRANT_RUNES :57-73 (15-entry block table ▘…█), aspect() :253-255 (null→default), withAspect :938 (+B5 strict-throw guard :940-945), withSmoothRim :978 (default OFF preserves legacy bytes), smoothRimCell sampling :383-442, segmentAt :458-473. GaugeCircle port inventory (reconD §3, ≈25-30 distinct sites / ~150-180 net LOC): aspect division at arc y :101, tick-outer y :129, tick-inner y :138, needle y :152 (parametric ÷aspect idiom — Donut :670-674 precedent); annulus rework — arc stamping loop :96-117 becomes per-cell x/y scan with sqrt ring test (~25 lines) + smoothRimCell analogue (~55) + atan2→angleRatio≤ratio segment analogue within the 0.75π→−0.25π sweep (~18); grid becomes per-cell ['char'=>string,'color'=>?Color] (7 write sites + string-conversion pass :163-184 rework) because quadrant runes need per-cell color identity (current pass keys colors off runes via in_array '●'/'○').

**Design**: ctor +2 params **APPENDED after labelColor**: `?float $aspect = null`, `bool $smoothRim = false` (9 positional call-sites constraint from D2 — absolute); ::new() carries defaults; `private function aspect(): float { return $this->aspect ?? self::DEFAULT_ASPECT; }` with `DEFAULT_ASPECT = 2.0` — **null⇒2.0, MATCH DONUT default-ON per R2** (the human ruling overrides the 1.0-legacy recommendation — gauge default output MOVES, see Declared-output-change flag); consts copy-or-share: DEFAULT_ASPECT(2.0)/RIM_SAMPLES/QUADRANT_RUNES duplicated private (DRY extraction into trait = out-of-ceiling, note-only); NEW public withAspect (B5 guard mirror — non-finite/≤0 throws InvalidArgumentException, message idiom mirrors Donut :940-945 adapted "gauge aspect ratio") + withSmoothRim(bool $on = true), both following the D2 allocation-preserving pattern; ALL 11 withers propagate aspect/smoothRim; smoothRim default OFF ⇒ ●/○ C8a predicate + all rune tests live untouched (quadrant runes strictly opt-in); **DECLARED-OUTPUT-CHANGE (R2)** — aspect default 2.0 ⇒ the DEFAULT PATH (no setSize/no withAspect/no smoothRim) RE-RENDERS: gaugeCircle goldens ×2 RE-RECORD at this step — the SECOND re-record (PRE-image = D2's just-shipped post-bodies; targeted reflection replay only; cross-check replay==targeted-suite PASS); legacy-pin role MOVES to the `withAspect(1.0)` escape test — geometry at 1.0 == D2-shipped bytes (replaces the old default-freeze gate); B5-guard message-parity tests stand. Also: fix Donut.php:99 stale ARC_CHARS provenance docblock — replace the false "Copied verbatim from GaugeCircle::ARC_CHARS" sentence with truth (source const deleted by B12) [explicit 1-line ceiling addition, prose-only — R5-iii authorized].

**Write-set ceiling**: sugar-dash/src/Plot/Chart/GaugeCircle.php [SYMBOLS: ctor+::new(), consts block, aspect()/withAspect/withSmoothRim NEW, arc loop, tick loops, needle, grid→per-cell struct + string pass, getInnerSize, all withers], sugar-dash/tests/Plot/Chart/GaugeCircleTest.php, sugar-dash/src/Plot/Chart/Donut.php [SYMBOLS: ARC_CHARS docblock :96-102 provenance sentence ONLY], sugar-dash/tests/golden/{80x24,120x40}/gaugeCircle.golden [SYMBOLS: body RE-RECORD ×2 — second re-record per R2]; ALL other goldens incl. plot-braille ×2 FORBIDDEN-churn (must stay EMPTY-delta).

**Verify**: **PRE-image capture** (post-D2 goldens sha + `new(0.8)`/`::new(80)` render shas) — gaugeCircle.golden ×2 EXPECTED to move this step (DECLARED per R2: targeted reflection replay only, PRE-image = post-D2 shipped bodies, cross-check replay==targeted-suite PASS, double-replay sha determinism, bodies cross-dim DIFFER); **legacy-escape pin**: `withAspect(1.0)` render bytes == PRE-captured D2-shipped default bytes (this replaces the former default-freeze gate); delta +3..5 tests (opt-in smoothRim render runes ⊂ declared QUADRANT_RUNES set, no ●/○ rim leak when ON; withAspect strict-throw parity — non-finite/≤0 both die with message idiom; withAspect/withSmoothRim immutability + allocation-survival pins; legacy-escape byte-pin); full dash rc0 band floor-from-D2 +3..5 tests; Donut suite untouched-green (docblock-only); census 125 UNTOUCHED.

**Declared-output-change flag**: YES (per R2) — default aspect 2.0 ON: gaugeCircle goldens ×2 second re-record + default-render geometry shifts vs post-D2; `withAspect(1.0)` legacy escape pinned byte-identical to post-D2 state. Donut.php prose-only.

### D4 (Lane B) sugar-dash: Sunburst cell-width wiring to Core\Util\Width [premise CHANGED] [R5]

**Why**: the C3-era docblock claims — :269-271 "no wcwidth machinery exists repo-wide (chart_v4 Parked (c))" and :396-397 "(no wcwidth repo-wide…)" — are **FALSE TODAY**: candy-core/src/Util/Width.php is production (1028 LOC; Width::string/of — East-Asian+emoji=2, zero-width=0, ANSI-stripped, memoized; isWide UAX table :964; truncate/pad/wrap family; live probe Width::string('日本')=4). sugar-dash already requires sugarcraft/candy-core and imports Core\Util\Width 112×; sugar-charts also requires it (1×). Sunburst is simply UNWIRED: center window (:289-296) counts 1 rune = 1 cell so a CJK label overflows the circle by its extra columns; legend budget (:355-374), advance (:387 `$legendX += $visibleWidth + 2`), and border pad (:398-399 mb_strlen of stripped) all count CODEPOINTS not CELLS → wide-glyph entries desync the box border. Zero sunburst goldens and zero examples exist (census @d549c0d48) ⇒ **zero golden surface**.

**Design**: center label window: walk the mb_str_split codepoints accumulating Width::of per glyph — the window becomes CELLS (a double-width glyph occupies 2 cells, written once at its lead column; builder mirrors current offset arithmetic `intval($centerDiameter/4)` semantics in cell-space). Legend: `$visibleWidth = Width::string($visible)` (:356 mb_strlen swap); over-long entry truncation via Width::truncate to the remaining CELL budget (replaces mb_substr window :361-374 area); advance/pad switch to Width::string (pad math: `$width - 2 - Width::string($legendLineStripped)`). ASCII behavior byte-IDENTICAL by construction: Width::string == mb_strlen for BMP-narrow runs — **pin via PRE sha1 tests (C3 capture pattern): record ASCII-fixture render shas PRE-EDIT, assert equality POST** (per-golden verify not applicable — none exist). Replace the two false docblocks with honest cites (machinery = SugarCraft\Core\Util\Width, wired this step). No composer change (dep already required — path-repo policy: no repositories[] ever).

**Write-set ceiling**: sugar-dash/src/Components/Tree/Sunburst.php [SYMBOLS: center window :263-296 region, legend builder :355-374, advance :387, pad :396-399, docblocks :269-271 + :396-397, use-statement], sugar-dash/tests/Components/Tree/SunburstTest.php ONLY.

**Verify**: full dash rc0; delta +3..5 tests (CJK center fits cell window — no glyph bleeds past the rim box; wide legend entry truncates-to-cells + border stays column-aligned; ASCII pin(s) sha PRE==POST byte-identity; cell-vs-codepoint distinction test: '日本' advances 4 not 2); goldens: NONE exist ⇒ porcelain shows ONLY the 2 ceiling files; SunburstTest pre-count (measure at spawn) + delta exact.

**Declared-output-change flag**: YES-multibyte-only / NO-ASCII (bug-class, R5; CJK renders correct cells; ASCII byte-paths pinned).

### D5 (Lane B) sugar-dash: Bubble identifier rename — shape family [R3]

**Why**: all three symbols are PRIVATE (verified @d549c0d48: `private const CIRCLE_CHARS` :53, `private function drawBubbleOnGrid` :406, `private function getCircleChar` :438) ⇒ zero public-API churn risk; v4 Q1 parked the rename WITH the "identifier stays" clause — this step completes it (prose already C5/S1-amended to shape-family truth; identifiers lag). Census: Bubble.php 15 src hits (CIRCLE_CHARS :51/:53/:389/:397/:434/:441/:444/:447/:450/:453; drawBubbleOnGrid :387/:390/:406; getCircleChar :420/:438); BubbleTest.php: :437 section comment, :449 docblock, **:459 BY-NAME reflection string pin 'CIRCLE_CHARS'**, **:467 fail() message string**; zero hits examples/README/docs/.tape/sugar-charts/candy-*. History docs (chart_plan/worklog/v4/backlog) NEVER rewrite. Recon options on record (Design fixed to the ruling): Set1 glyph-family (BUBBLE_GLYPHS/bubbleGlyphFor), Set2 shape-family (SHAPE_GLYPHS + getShapeChar-recon / shapeCharFor-brief), **Set 3 full sweep (ROUNDED_BOX_GLYPHS/glyphAtOffset/drawShapeCluster) — RULED IN per R3** (the earlier "drawBubbleOnGrid is a fine domain word" preference overruled; Set 3 also pulls the class-header vocabulary sweep :14-27).

**Design** (Set 3 per R3): CIRCLE_CHARS→ROUNDED_BOX_GLYPHS, getCircleChar→glyphAtOffset, drawBubbleOnGrid→drawShapeCluster. Pure mechanical rename across ALL 15 src refs + the 4 BubbleTest sites — by-name pins :459 (constant-name reflection string 'CIRCLE_CHARS'→'ROUNDED_BOX_GLYPHS') + :467 (fail() message string) — and every prose site; class-header vocabulary sweep :14-27 if any renamed identifiers appear there; docblock :51 clause "(the CIRCLE_CHARS identifier stays per the v4 Q1 ruling)" REPLACED by rename-completion note ("renamed from CIRCLE_CHARS in v5 D5 — the table drives shape-family glyphs, never true circles"); :434-436 "Resolved through CIRCLE_CHARS" prose lands on ROUNDED_BOX_GLYPHS; :420/:438 call/def follow glyphAtOffset, :387/:390/:406 follow drawShapeCluster. History docs NEVER touched.

**Write-set ceiling**: sugar-dash/src/Plot/Chart/Bubble.php [SYMBOLS (Set 3): CIRCLE_CHARS const+uses, getCircleChar def/call, drawBubbleOnGrid def/calls, class-header vocab :14-27, docblocks :51/:434-436], sugar-dash/tests/Plot/Chart/BubbleTest.php [SYMBOLS: :437 comment, :449 docblock, :459 by-name pin string, :467 fail-msg] ONLY.

**Verify**: **PURE-RENAME byte-zero-movement gate** — full dash counts IDENTICAL to spawn-time floor (5924/9621/1Sk/0W baseline + siblings' declared deltas; Tests/Assertions/Skipped UNCHANGED by this step, rc0) + Bubble render sha1 pin before==after (existing 42-test render corpus is the pin; declare the counted set); absence proofs: `/usr/bin/grep -rn 'CIRCLE_CHARS\|getCircleChar\|drawBubbleOnGrid' sugar-dash/src sugar-dash/tests` → ZERO hits (history .md files keep theirs by design); porcelain EXACTLY 2 files; census 125 UNTOUCHED.

**Declared-output-change flag**: NO (private identifier rename, zero byte movement).

### D6 (Lane A, disjoint) sugar-dash: plot-braille header honesty [R4]

**Why**: reconE [C2] measured — post-C6′ the left ($dotPlot :21) and right ($braillePlot :27) panels render BYTE-IDENTICAL curves (both default MODE_LINE braille; `===` true), while header echo :37 `str_pad('MarkerDot', $W) . '  ' . 'MarkerBraille'` advertises a nonexistent contrast: **"MarkerDot" exists NOWHERE as code** (no class/const/method in src; Plot.php:24 prose only). The echo is ON the output path — both goldens embed the false header at body line 5 (80x24 + 120x40 plot-braille.golden:5, verified live) ⇒ ANY label change = output change. The nearest real knob: withMode(Plot::MODE_SCATTER) (MODE consts :32-33, withMode :63, line-vs-scatter branch :183/:344 — scatter = unconnected setPoint dots).

**Design (variant (c) REAL COMPARISON per R4)**: docblock :7-15 + section comments :18/:24 made honest AND the panels genuinely contrast — left panel `->withMode(Plot::MODE_SCATTER)` (immutable-assign discipline; setPoint dots, recon cites Plot.php:183-187), right panel STAYS default line; echo :37 labels → truthful pair e.g. `str_pad('Scatter (MODE_SCATTER)', $W) . '  ' . 'Line (default)'`; goldens plot-braille ×2 RE-RECORD via targeted reflection replay — bodies now genuinely differ PANEL-SIDE (not just dims): header row + left curve rows move, right curve rows byte-unchanged; **builder MUST live-verify scatter renders recognizably alongside line — STOP + report if MODE_SCATTER output is broken/degenerate** (do NOT silently downgrade; orchestrator re-rules). C6′ assign lines UNTOUCHED. (The (i) docblock-only / (ii) echo-relabel ladder is superseded by the ruling — retained only as the rejected options record.)

**Write-set ceiling**: sugar-dash/examples/plot-braille.php [SYMBOLS: docblock :7-15, comments :18/:24, echo :37, left chain :19-21 (withMode assign)] + sugar-dash/tests/golden/{80x24,120x40}/plot-braille.golden ONLY.

**Verify**: `php examples/plot-braille.php` rc0 + scatter panel LIVE-verified recognizably (dots present, non-degenerate) else STOP + report; stdout delta = label row(s) + left-panel curve rows (right curve rows byte-unchanged); targeted `--filter plot-braille` rc0 on new goldens; double-replay sha1-identical; full dash rc0, delta **+0 tests** (example data, no new test files); goldens porcelain EXACTLY plot-braille ×2; zero other-golden churn (gaugeCircle ×2 belong to D2/D3 only).

**Declared-output-change flag**: YES (per R4 variant (c)) — labels + left panel shape; goldens ×2 re-record.

## D-board

| Step | Item | Lane | Build | Review loop | Commit | SHA |
|---|---|---|---|---|---|---|
| D1 bufferFromOutput row-clip [R1] | Chart.php diff geometry | B | ✅ | ✅ | ✅ | a7d1cffc2 |
| D2 GaugeCircle setSize activation | GaugeCircle | A | ✅ | ✅ | ✅ | 69f6d3450 |
| D3 GaugeCircle aspect+quadrant [R2] | GaugeCircle/Donut:99 | A | ⬜ | ⬜ | ⬜ | — |
| D4 Sunburst Width wiring [CHANGED premise] | Sunburst/Width | B | ⬜ | ⬜ | ⬜ | — |
| D5 Bubble identifier rename [R3] | Bubble | B | ✅ | ✅ | ✅ | ce72d30ba |
| D6 plot-braille honesty [R4] | example+goldens | A | ⬜ | ⬜ | ⬜ | — |

(Only commit agents tick this board.)

## Lanes & ordering

**True file-disjointness**: D1 (src/Plot/Chart/Chart.php + ChartTest) · D2/D3 (src/Plot/Chart/GaugeCircle.php + GaugeCircleTest + gaugeCircle goldens + gaugeCircle example; D3 adds Donut.php 1-line docblock) · D4 (src/Components/Tree/Sunburst.php + SunburstTest) · D5 (src/Plot/Chart/Bubble.php + BubbleTest) · D6 (examples/plot-braille.php + plot-braille goldens). The five groups are MUTUALLY DISJOINT; only D2→D3 are same-file serial. **Lane A = D2 → D3 serial ∥ D6 (disjoint, may interleave). Lane B = D1 → D4 → D5 (arbitrary order within lane — disjoint).** **GaugeCircle goldens double-re-record sequence (R2): D2 re-records gaugeCircle ×2 FIRST (activation, aspect-NEUTRAL raw-cell bodies), D3 re-records them AGAIN SECOND (PRE-image = D2's shipped bodies, aspect-2.0 defaults) — the serial D2→D3 order is load-bearing for that PRE/POST chain; D6's plot-braille ×2 re-record is independent of both.** Cap: max 2 concurrent builds — one per lane; commits serialized by the orchestrator; NEVER push; direct master. Sibling dirty lanes are invisible across libs (charts untouched by v5 entirely; dash gates must attribute foreign-delta per v4 precedent).

## Parked / VOIDED

- **VOIDED-PARK PERMANENT — locale/env auto-detection of glyph sets**: environment-dependent output violates the byte-identity acceptance contract (opt-in flags only; T::charset() [C9] remains API-surface, zero consumers — evidence stands unchanged).
- **bufferFromOutput WIDTH-clip drop** (full-width parity so runes past $width enter the buffer) — PARKED; D1 fixes the ROW leg only; ChartTest :994-995 "status quo" mirror stays; larger buffer + no known delta-hole on the width axis.
- **Donut aspect/quadrant** — SHIPPED (v3/v4); Donut.php is reference for D3 (single :99 docblock fix only).
- **gauge example ratio-clamp oddity** — `GaugeCircle::new(80)` clamps to ratio 1.0 → always-full ring: upstream API semantics, not ours. PARKED.
- **candy-buffer/src/Buffer.php:120 wcwidth TODO** — now genuinely satisfiable via Core\Util\Width (D4 discovery); wiring it inside candy-buffer is a candy-core/Buffer blast-radius job — **v6 candidate**.
- **Plot::MARKER_BRAILLE ('braille', Plot.php:30)** — dead-ish public const vestige post-B12 (grep: only prose :24 + PlotTest:111 pin); removal = public API churn — needs its own v6 ruling. PARKED.
- Sunburst MARKER/glyph extras (MarkerDot headers) — the "MarkerDot" fiction dies inside D6 (example+goldens); no code symbol ever existed ⇒ nothing else to fix.
