# Chart v6 plan — buffer/gauge/width/marker leftovers (round v6)

Protocol: chart_prompt.md v3 machinery, UNCHANGED. Ledger: chart_worklog.md (append-only, authoritative; this board is a cache; git is the authority). Intake recon @HEAD 12f956ada, tree porcelain EMPTY, origin/master = 12f956ada (v5 close-out already synced by foreign lane; NEVER-push standing retained — we do not push regardless). Evidence: /tmp/v6-recon-{0,f1,f2,f3,f4}/evidence.txt (+ probe files). All four premises RE-DERIVED live at intake — verdicts inline per step.

## Steps (v2-contract)

### F1 — diff-buffer COLUMN parity for Chart re-renders (sugar-dash)
**Why:** D1 (a7d1cffc2) closed the ROW leg of the stale-glyph class; the COLUMN leg remains. bufferFromOutput stores only the first $chartWidth runes/row (clip loop @Chart.php:823; Buffer::new($width,$height) @:817) while colored frames emit up to 50 runes into a 40-wide chart (probe1: default colored bar rows = 50, axis 48, line 49). Consequences measured: mutation exclusively at cols 40–46 ⇒ delta strlen=0 while a fresh full frame carries the glyphs to stdout; stored buffer physically lacks painted columns (cellAt(40,11) throws). Cols ≥ cw freeze at first-frame paint — identical class to D1. Premise TRUE (latent: zero production consumers of the multi-frame diff path today — public withers reset context; reachable only via reflection-mutated dataPoints, as ChartTest does). D1 precedent = fix latent class before wiring.
**Design (gated on R1=fix):** $diffWidth = max($chartWidth, E) + 10 where E = n·(max(1, ⌊$chartWidth/n⌋ − 1) + 1), n = count($dataPoints). Recon-verified exact-or-+1-slack for every probed n (2→50, 7→50 vs 48, 30→70 exact); the +10 gutter is already advertised by getInnerSize():483 (full emitted frame = width+10). Both bufferFromOutput call sites (~:190/:198, beside D1's $diffHeight) pass $diffWidth. GATE CHANGE (hard requirement — diffWidth depends on dataCount which the old chart-area gate never carried): full-frame fallback must compare ACTUAL previousFrame->width()/height() instead of prevWidth/prevHeight chart-area keys — without it a dataCount change on a context-keeping instance makes Buffer::diff() THROW (probe F demonstrated the crash; Buffer.php:416–422 dim-mismatch throw). Residual: yLabels > 8 chars (|v| ≳ 1e10) exceeds the bound (probe G, 52-col rows) — a regime where the rendered frame itself already misaligns (pre-existing); clip to $diffWidth there, document, out of scope.
**Write-set ceiling (SYMBOLS):** sugar-dash/src/Plot/Chart/Chart.php — bufferFromOutput signature+clip, BOTH call sites, resize gate (prevWidth/prevHeight → actual-dims compare), new private diffWidth()/equivalent. sugar-dash/tests/.../ChartTest.php — mirror moves (LIVE line numbers from recon): :921-932 colored parity 40→diffWidth grids + status-quo comment rewrite · :955 direct invoke 40→diffWidth · :963 cellAt(39,10) pin · :970-971 census loops col<40→50 · :799-800 plain parity (40,11)→50-grid · :1011-1012 plain invoke →50; :993-999 synthetic (28,3) invoke STAYS (the only surviving clip-is-real pin). NEW tests: (a) beyond-width (<diffWidth) mutation ⇒ non-empty delta covering those cols (probe-B class); (b) dataCount change forces full frame, not throw. STOP-and-report if ceiling exceeded.
**Verify:** full dash suite rc0 (baseline from fresh pre-flight row; expect +2..+4 T). GOLDENS + full-frame stdout BYTE-FROZEN (diff-internal change only): sha census of ALL dash goldens + a pre-captured full-frame colored stdout pin taken PRE-edit, re-measured at gate — zero movement tolerated.
**Flags:** declared output change: delta-stream contents only (diff-internal), pre-approved discipline R5.

### F2 — gaugeCircle example clamp fix + third golden re-record (sugar-dash)
**Why:** examples/gaugeCircle.php:8 `GaugeCircle::new(80)` — ratio param is 0..1, ctor clamps (GaugeCircle.php:43 max(0.0,min(1.0,$ratio))) ⇒ 80 ≡ 1.0 ⇒ committed goldens render a FULL ring labeled "100%" (●43/○0, ●83/○0) where intent is 80%. API contract-correct (documented clamp); the EXAMPLE is the bug. Premise TRUE. Goldens are a LIVE instrument (GoldenSnapshotTest SKIPPED map :45–75 has NO gaugeCircle entry; --filter gaugeCircle = OK 48/112, both ✔ compare).
**Design (gated on R2):** example:8 → `new(0.8)`. Re-record gaugeCircle goldens ×2 via TARGETED reflection replay ONLY (GoldenSnapshotTest::runExample + regen-branch header mirror; blanket generate-goldens and phpunit --regenerate FORBIDDEN; harness pattern /tmp/d2-replay.php, b10a recipe: atomic tmp+rename, double-replay sha-equal, real==scratch). Predicted post-fix bodies (recon replay, /tmp/v6-recon-f2/): 80x24 e1e1060f…, 120x40 7f6f2116… (●34/○9 · ●65/○18 · label "80%"). Harness TRUTH: patchExample regex (:147–150, limit 1) OVERWRITES setSize(60,15) with the golden dims — example's setSize(60,15) line stays; literal-path probe of new(0.8)->setSize(60,15) = 14-row 80% ring (c1b4e2ea). MUST also bump examples/generate-all.php:68 — the meta-generator EMBEDS the literal 'GaugeCircle::new(80)' and would regenerate the bug. R2 choice covers GaugeCircleTest.php:433/:437 $example80/$example120 byte-pins: re-derive against 0.8 chain (R2=fix, keeps the comment's claim true) vs amend-comment-only (R2=fix-min).
**Write-set ceiling:** sugar-dash/examples/gaugeCircle.php (:8), sugar-dash/tests/golden/80x24/gaugeCircle.golden + 120x40/gaugeCircle.golden (bodies+header mirror), sugar-dash/examples/generate-all.php (:68), + [R2=fix only] sugar-dash/tests/Plot/Chart/GaugeCircleTest.php (:433,:437). STOP-and-report if exceeded.
**Verify:** --filter gaugeCircle OK rc0 live-compare; committed golden bodies == predicted shas (e1e1060f/7f6f2116 — flag loudly if live replay diverges: something else moved); full dash suite green, T unchanged; golden sha census: ONLY gaugeCircle ×2 move (all others — plot-braille bodies 42aa1d59/0dcc7072, sunburst, D-era pins — byte-frozen).
**Flags:** DECLARED golden change gaugeCircle ×2 (third re-record) + example visible stdout change (intended 80% ring). Current-committed full-file shas (frozen evidence): 89a6d79d124cfb3e… / f884fae6ea41913d…; bodies 0ed4a4f3 / c836853b.

### F3 — Buffer.php:120 wcwidth TODO disposition (candy-buffer)
**Why:** v5 parked as "candy-core/Buffer blast-radius job". INTAKE RE-READ: the cycle premise is FALSE as stated — candy-buffer runtime require = php ^8.3 ONLY (ext-intl/react/candy-pty edge is candy-CORE's), candy-core does NOT require candy-buffer ⇒ buffer→core import creates NO new composer cycle. The real cost is dependency drag: a dep-free leaf would transitively pull candy-pty/ext-ffi/react+stream into its 15 runtime consumers. Structural finding: the TODO sits inside fromString() — documented test-fixtures-only ("not for accurate terminal state replay"), iterates BYTES (multibyte CJK already misparses byte-per-cell regardless of the width line ⇒ width-fix inside the byte loop is a half-measure serving nothing), callers = candy-buffer/tests + sugar-veil/tests ONLY, zero src/. The wide-char model ALREADY EXISTS and is blessed: Cell width 1/2 + continuation 0, Buffer::diff skips continuations (:439–442), REP-guard width===1 (:533), suite tests them (CellTest/BufferTest wide cases). ASCII default untouched by all v6.
**Design (gated on R3):** R3=doc → replace the :120 TODO (and align the :63-70 fromString docblock) with an honest design note: display width is a Cell-construction-layer property; the canonical measurement primitive is SugarCraft\Core\Util\Width (candy-core, STANDALONE — zero imports, UAX isWide + ANSI-aware; imported runtime = ext-ffi drag for 15 consumers, ruled out this round); fromString stays byte-loose by contract; full UTF-8 tokenizer inside fromString = separate future candidate (blast: candy-buffer + sugar-veil test fixtures). Comment/docblock bytes ONLY, zero behavior change. R3=void → no edit at all; VOIDED-PARK-with-pointer recorded here. R3=hook → static default-internally width-provider (1 static prop + 1 line; mechanically feasible per 101-site census with zero call-site churn) — evidence-AGAINST: adds unused production API to serve a test-only byte-loose parser. R3=import → runtime Width import — evidence-rejected (dep-drag).
**Write-set ceiling:** candy-buffer/src/Buffer.php — COMMENT/DOCBLOCK bytes only (region ~:63–70, :116–122).
**Verify:** full candy-buffer suite rc0, T/A IDENTICAL (counts must not move); git diff -w word-check = comment-only (no code byte changed; gate shows diff hunks in comments only). candy-core untouched.
**Flags:** none (R3=doc/void).

### F4 — MARKER_BRAILLE vestige disposition (sugar-dash)
**Why:** ZERO code readers repo-wide (full /usr/bin/grep census @12f956ada): definition Plot.php:30 + sole test pin PlotTest:111; every other hit is prose/historical (Plot.php:24 docblock — FALSE post-B12: "Default uses MarkerBraille for smooth curves" describes a knob that no longer exists; README.md:525 stale tile = docs lane, never-stage; docs/_data + docs/lib generated pages ZERO hits). Git forensics: MARKER_BRAILLE's only lifetime reader was the write-only $marker default (B11); $this->marker was NEVER read in render; B12 (4b27b4ff9) deleted MARKER_DOT/$marker/withMarker — deletion total (zero code hits). MODE_LINE/MODE_SCATTER are the live, fully-wired concept. WIRE option has NO target (no dot-fallback renderer exists; wiring is arbitrary AND contradicts the v5-Q5 ruling that killed the marker knob).
**Design (gated on R4=delete):** remove `public const MARKER_BRAILLE` (Plot.php:30, collapse :31 blank), remove false docblock line Plot.php:24, remove now-100%-vacuous testMarkerConstants (PlotTest:109–112, whole method — single assert on the deleted symbol). Adjacent cosmetics (banner :114–116, testMarkerBrailleDefault name/comment :118–125 — body survives green) per R6: minimal=untouched / tidy=rename+comment only. Public-API churn justified under explicit ruling with B12 precedent (pre-1.0).
**Write-set ceiling:** sugar-dash/src/Plot/Plot.php (:24,:30–31) + sugar-dash/tests/Plot/PlotTest.php (:109–112 [+R6]).
**Verify:** full dash suite rc0, T −1 / A −1 vs baseline (band: exactly one method removed — any other movement = finding); goldens/examples untouched (zero coupling verified); README:525 NOT staged.
**Flags:** test-count change DECLARED (−1T/−1A).

## F-board (tick at commit; board=cache of ledger)
| step | lane files | status | ruling gate | sha |
| F1 | dash Chart.php+ChartTest.php | ✅ COMMITTED 2026-09-04 (ledger holds sha) | R1 (fix/doc/park), R5 | — |
| F2 | dash gaugeCircle.php+goldens×2+generate-all.php[+GaugeCircleTest.php] | PENDING | R2 (fix/fix-min/park) | — |
| F3 | candy-buffer Buffer.php (comments only) | PENDING | R3 (doc/void/hook/import) | — |
| F4 | dash Plot.php+PlotTest.php | ✅ COMMITTED 2026-09-04 (ledger holds sha) | R4 (delete/leave/wire), R6 | — |

## Rulings
Rulings received 2026-09-03T23:35Z via orchestrator question set — ALL steps cleared for build.

- **R1 (F1) = FIX** — full-column parity: $diffWidth = max($chartWidth, E) + 10 with E = n·(max(1, ⌊$chartWidth/n⌋ − 1) + 1); resize gate switches to comparing ACTUAL previousFrame buffer dims (Buffer::width()/height()) so a dataCount change falls back to full-frame instead of Buffer::diff dim-mismatch THROW; apply the ChartTest mirror move-list per F1 write-set. Full-frame stdout/goldens byte-frozen.
- **R2 (F2) = FIX** (not fix-min) — examples/gaugeCircle.php:8 new(0.8); gaugeCircle goldens ×2 THIRD re-record via targeted reflection replay ONLY (recon-predicted bodies 80x24 e1e1060f…, 120x40 7f6f2116… — final bodies must byte-match these or the step reports the divergence with explanation); examples/generate-all.php:68 embedded literal must be bumped (else regeneration reverts the fix); GaugeCircleTest.php:433/:437 example byte-pins re-derived for the new bodies.
- **R3 (F3) = DOC** — widen Buffer.php:120 TODO with honest pointer: SugarCraft\Core\Util\Width exists and is dependency-standalone; candy-buffer runtime require is php-only so importing core is dependency-drag (ext-ffi/pty/react onto its consumers), NOT a cycle; fromString is a test-only byte-wise factory; Cell model already width-blessed on real paths. No composer change, no import, no callback hook. VOIDED-PARK (locale auto-detect) unchanged.
- **R4 (F4) = DELETE** — remove Plot.php:30 MARKER_BRAILLE const, Plot.php:24 false docblock line, and the whole vacuous PlotTest.php:109-112 testMarkerConstants method. Expected dash delta −1T/−1A (5940/9709 → 5939/9708). This ruling AUTHORIZES the removals within step F4 (GR-12 note).
- **R5 = YES** — sha-freeze discipline adopted: ASCII/default-path output pins captured PRE-edit per step; full-frame stdout/goldens byte-frozen repo-wide EXCEPT gaugeCircle ×2 under R2; F1's only authorized output change is diff-internal delta streams, demonstrated by new mirror tests, never by golden movement.
- **R6 = MINIMAL** — cosmetics excluded from all write-sets: testMarkerBrailleDefault rename, banner-comment relabeling, README:525 stale tile, historical docs prose — all SKIPPED this round (not findings; docs-lane never-stage stands).

## Lanes (intake-verified disjoint @12f956ada)
F1 = Chart.php+ChartTest.php · F4 = Plot.php+PlotTest.php · F2 = examples/gaugeCircle.php + golden/80x24 + /120x40 gaugeCircle + examples/generate-all.php [+ GaugeCircleTest.php per R2] · F3 = candy-buffer/src/Buffer.php. ALL FOUR file-disjoint (F1 vs F4 confirmed: no shared files; F2's goldens vs F1/F4 gates → sibling-attribution rows, precedent B7/B10b/D1). Max 2 concurrent; commits serialized one-at-a-time.

## Parked / VOIDED (v6 intake + inherited)
- VOIDED-PARK (permanent, v5): locale/env auto-detect in goldens naming — byte-identity collision.
- PARKED v6-intake: fromString() full UTF-8 tokenizer rewrite (F3 residual; blast = candy-buffer + sugar-veil test fixtures — needs own round ruling).
- PARKED v6-intake: production caller-side width=1 hardcodes for CJK (Chart.php:822-824 + sugar-reel/boxer/veil/table/calendar grid-fill sites) — CJK-label diff-op column misplacement is pre-existing CALLER-side, Buffer is ready (Cell model blessed); future dash-width round.
- PARKED v6-intake: yLabels>8-char overflow regime frame misalignment (pre-existing visual bug, F1 clamps there).
- OUT-OF-LANE: README.md:525 stale gaugeCircle/MARKER_BRAILLE tile prose + docs churn — foreign/docs lane, never-stage.
- v5 VOIDED list unchanged.
